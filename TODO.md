# TODO

**Scope:** this repo is no longer just a landing page prototype. The whole getirfinans.com website gets built here, page by page. Next page after the landing page: the debit card page (`/hesap-karti`). **paranaiyibak.com now lives here too**, under `/pnib` — a second brand on the same codebase, sharing `src/styles/tokens.css` and nothing else.

## Next up

- [x] **Hesap kartı — mobile page feedback, 4 items (2026-08-14, on `main`, reviewed at 390×844)** — all four done. Verified in headless Chromium at 320/390/767/900/1440 plus both `prefers-reduced-motion` states. Page is **773px shorter at 390** (8778 → 8005).
  - [x] **Steps section → swipe deck (item 1).** Was **1505px at 390 — 1.8 viewports for three steps** — a full-width vertical stack under an oversized phone. Now **990px**. The phone went 256 → `min(200px, 52vw)`: it is `aria-hidden` decorative and its inner app UI is illegible at phone width, so it was spending ~380px of height as texture. The three items became a horizontal scroll-snap deck of white `--r-lg` cards inside the lilac band, reusing the landing's **`.rates__cards`** device (bleed past the gutter, snap on x, hidden scrollbar, next card peeking) rather than inventing a pattern — so no dots are needed, the peek *is* the affordance. Mode is already `off` at ≤767, so none of the desktop pin/scrub machinery is involved. Three notes for next time: **`grid-template-columns` must be reset to `none`** or the base grid keeps placing items and the flex row never engages; the bleed unit is **`--dpc-panel-pad`, not `--gutter`**, because the deck's edges are the band's; and the band's `overflow: clip` is what makes cards scroll away under the rounded corner.
  - [x] **Sanal button spacing (item 2).** The 18px box gap was rendering a **6px visual gap**. Cause: this section overrides the shared mark to `bottom: -0.36em` to clear "güvenle"'s descender, which hangs the yellow bar **12px below the title's box** — so the CTA sits under the *bar*, not the text. Now a 36px box gap ⇒ 24px visual (`--sp-xl`). **⚠ The `.dpc-sanal__head` comment claiming "18px box gap renders a 16px VISUAL gap … overhangs by 4px" is stale** — it predates the descender fix and holds only where the shared mark applies.
  - [x] **Sanal card reduced (item 3).** Was 234×355 with a **414px bounding box** — `rotate(-20deg)` adds ~59px of layout height that the width alone doesn't predict. Now 170px wide / ~301px bounding, which lifts the benefit cards ~136px; **two of the three now land in the first screen** (first card at offset 565, was 728). Section 1178 → 1051. The 380px glow dropped to 260 so it doesn't read as fog around a smaller card.
  - [x] **Abroad mirrors sanal (item 4).** City photos hidden, the horizontal strip **deleted** rather than hidden in place (leaving it meant a scroll container, snap axis and 256px height still applying to one centred card), reordered to title → card → points, and the card shares one `--dpc-card-showcase-w` with sanal. Both measure **exactly 301px** rotated. Scope is ≤767 only: 768–920 keeps the absolute collage and ≥921 still runs the handoff, whose landing pad is this same `.dpc-abroad__img-card`.
  - **⚠ Three traps worth keeping, all found by measuring rather than reading:**
    1. **`.dpc-abroad__img-card` IS the `motion.div`** (CardSlot), so Motion's inline `transform` for the scale entrance beats the stylesheet — a CSS `rotate` on it silently did nothing (258px bounding vs sanal's 301px). `.dpc-sanal__stack` works because it's a plain div with Reveal on its *parent*.
    2. **Rotating the `<img>` instead is also wrong** — `.t-tilt-card` is `overflow: hidden`, so it cropped the rotated art to the unrotated rectangle and **the card rendered as a hexagon**. The fix was a new **`--tilt-base-rotate`** var inside the shared `.t-tilt-card` transform chain (defaults to `0deg`, so every other caller is byte-identical), which turns the clip box and the glare with the card. `!important` on the outer box was rejected: it beats Motion but seizes the whole `transform` and kills the pop-in.
    3. **`prefers-reduced-motion` was destroying that rotation.** The rule was `transform: none !important`, which dropped the -20° and left the card at 258px inside a 301px reserved stage — a 43px hole. Now `transform: rotate(var(--tilt-base-rotate, 0deg)) !important`: a resting rotation is a static visual state, not motion, and reduced motion should soften movement without restyling the page.
  - **Also fixed, and it was self-inflicted:** hiding the step desc's `<br>` on mobile made the two copy halves run together as "uygulamadan**tek**" — the break was carrying the only whitespace. `DeliverySteps.tsx` now emits an explicit `{" "}`, which costs nothing on desktop because trailing whitespace before a forced break collapses.
  - **Two follow-ups deliberately not done here** (both would widen a mobile pass): the sanal head's gap bug **also affects desktop** — the overhang is em-relative, so at a 40px title it is 14.4px under an 18px gap ≈ **3.6px visual** — left alone because that comp is three review rounds old, so this is a question rather than a silent change. And **neither swipe deck is keyboard-reachable**: the steps deck and the landing's `.rates__cards` are both scroll-only, so steps 2–3 need a pointer. One `tabindex`/accessible-name pass should cover both.
  - **`npx ui-skills` is partly back**: `list` now works (bundled fallback), but `start` and every `get` still **403** on `ui-skills.com/skills/registry.json` — so the catalog bodies remain unavailable and the locally-pinned skills in `.claude/skills/` were used instead. Only `ui-skills-root` is cached locally; `baseline-ui` and the rest are not.

- [x] **Hesap kartı — mobile hero repaired (2026-08-14, on `main`)** — an external agent's mobile-hero pass was reworked. The *intent* was right (card video on top, copy beneath, full-width CTA, matching the landing hero's phone rhythm) and is kept; the execution had seven faults, all measured in real headless Chromium at 320/390/430/481/600/767 and 1440.
  - **⚠ The root cause, worth remembering: the landing hero's mobile box model was copied onto a structurally different hero.** `.hero`'s media is a full-bleed **background** behind its copy, which is exactly why `min-height: 100svh` + `justify-content: flex-end` works there — nothing stacks, so text can pin to the bottom of the viewport. `.dpc-hero`'s card video is an **in-flow sibling** of the copy, so heights add. Copied verbatim it made **954px of content in an 844px viewport and pushed the CTA 38px below the fold**. Fixed by letting the section size to its content (`height: auto`, no centring). Values ported, layout model not.
  - **A second `@media (max-width: 767px)` hero block** was added at the top of the file while the page's annotated mobile layer already carried hero rules at the bottom. The later (existing) block won on every conflicting property, which is why the new one's intent half-applied: `width: 100%` lost to `max-width: 450px`, so `border-radius: 0` landed on a **centred inset** card — square corners on a floating thumbnail. Now one block, in the existing mobile layer, per AGENTS.md ("consolidate when touched, don't grow it").
  - **Double top padding** — 100px on the section (existing) plus `92px + safe-area` on the container (new) plus a stray `margin-top: 32px` on a card that had just been reordered to *first* — ~224px of void above the product. One inset now, on the section.
  - **`hero__badge-stack` is a landing-page class and was borrowed onto this page.** It is unscoped (`sections.css` + `mobile.css`) and its geometry exists to hold the landing hero's **rotating multi-badge stack** — `min-height: 52px` for a 32px pill, i.e. 20px of dead space, plus a cross-page coupling where touching the landing badge animation silently moves the debit hero. Reverted to the page-scoped `.dpc-badge`; `Hero.tsx` is now byte-identical to HEAD.
  - **`.dpc-hero__text` was made a flex column, which it never needed** — the reorder happens on `.dpc-container`, its parent. Worse, as a flex child the `inline-flex` badge would stretch to full column width under the default `align-items: stretch`, turning the pill into a bar. Left as block; block layout shrink-wraps the pill.
  - **Three desktop regressions were smuggled into the mobile pass** and are reverted: `.dpc-hero` base height **840 → 960px** (a base rule, so ≥768 only), the removal of `.dpc-hero__text { margin-top: -80px }`, and the removal of `.dpc-badge { margin-bottom: 20px }` (the borrowed class was silently supplying 30px instead). Desktop is provably back: the whole CSS diff now sits inside the ≤767 block and 1440 measures 840 / 20px / −80px / 740×740 as before.
  - **Fixed a pre-existing bug found while measuring** (not the agent's): `.dpc-badge` is a fixed 32px pill but its label needs 293px, so it wraps and **spills below ~350px** — measured 39px of text in a 32px box at 320, second line hanging past the rounded edge. Same overrun `.hero__badge` already solves in `mobile.css`; fixed the same way (grow, don't clip).
  - **Two dead ends recorded so they aren't retried.** (1) Capping the full-bleed card by **`max-height`** looks right but `object-fit: cover` then crops the composition vertically and at 767 it **cut the card's own bottom edge** — the video's card fills more of its square frame than the white margin suggests. The cap belongs on width. (2) Centring the capped card with **`margin-inline: auto` collapsed it to 0×0** (measured at 600 and 767, hero fell to 420px): it is a flex item, so auto inline margins eat the free space and cross size falls back to content — which is 0, since everything inside is absolutely positioned. Use `align-self: center` with `width: 100%` + `max-width`.
  - **Hamburger was invisible on this page — white on white** (owner, 2026-08-14). `mobile.css` sets `.nav-toggle` to `#fff` below 767 because the **landing** hero is a dark photo, then flips it to `--gf-ink` on `.is-scrolled`/`.is-mega`. This hero is white, so the icon only appeared *after* scrolling — which is why it shows in section screenshots but not first-fold ones. Now `--gf-purple`, page-scoped to `body.dpc`: that is `icon/action/primary` (`#5D3EBC` in `semantic.Light.tokens.json`, and `icon/action/nav` is the same value), so it reuses the existing token instead of reintroducing the hex. Set for **all** states, not just unscrolled — this page's header is white or translucent-white throughout (`.is-scrolled` .92, `.is-mega` .98, `.mega` #fff), so purple holds at **7.3:1** everywhere and the icon needn't change colour mid-scroll like the landing's does. **⚠ The `.is-scrolled`/`.is-mega` selectors in that rule are not redundant** — `.is-scrolled .nav-toggle` is (0,2,0) and outranks a lone `body.dpc .nav-toggle` (0,1,1) no matter which file loads later. No media query needed: `.nav-toggle` is `display: none` above 920. Verified the landing page is untouched (still `#fff` unscrolled, `--gf-ink` in mega) and the toggle is unoccluded with the menu open.
  - **Result:** full-bleed 1:1 card at phone widths, re-centred at a 432px cap past 480 (radius returns with it). CTA inside the fold, no badge clipping, and `scrollWidth == viewport` at all six widths. `tsc --noEmit` clean; `npx eslint src/app/hesap-karti/` 0 errors (11 pre-existing `<img>` warnings). The `broken-image` design-hook finding on `debit-current.css` remains the known false positive — it matches the literal word `<img>` inside a CSS prose comment about `object-fit`.

- [ ] **Hesap kartı — page feedback round 3 (2026-08-11, on `main`, measured at 2504×1363)** — 6 items: 5 done, 1 deferred by the owner. Verified in real headless Chromium at 2504/1440/1145/921/820/768/390.
  - [x] **Both benefit lists split into one card per item** (items 2+3, Figma 22200:16926 sanal / 22200:16954 abroad). **Entirely CSS** — both comps draw the same card, so `.dpc-points` / `.dpc-point` carry it and neither component's markup changed.
    - Card is the comps' box exactly, and every value is already a token step: **320 wide, radius 16 (`--r-lg`), 16/24 padding (`--sp-lg`/`--sp-xl`), 16px icon→text (`--sp-lg`), 16px between cards, 40px glyph, 18/24 copy**. Verified 320×138 by `offsetWidth`/`offsetHeight` at 1440 and 2504.
    - **This supersedes the 2026-08-10 coherence pass**, and closes two questions it left open. `--dpc-point-w` tuned per list (264/240) is gone — the comp fixes both at 320, so **the "two panels are different widths" designer call (920 vs 576) is moot**: both rows are now n×320 + 16px gutters. `--dpc-point-lines` (the reserved text band) is gone too — equal heights now come from flex `stretch` + `justify-content: center` on the card, which is what the comp does. That also retires the 768–920 three-line bump **and** the harness lesson about `min-height` masking true line counts, since no `min-height` remains in this list.
    - **⚠ The comp's sanal card fill would have re-broken the CMO's "ortadaki madde okunmuyor" fix (2026-08-05) — read the note in `debit-current.css` before touching it.** CardHandoff flies a near-white card render (z 60) to viewport centre, exactly where the middle card sits. The comp draws `shadow/black/10` + 6px blur, which over `--gf-card-purple` composites to `#2c1e55` (the fill is a whisper; the 20%-white border draws the card) — but over a white traveler the same 10% black lands at `#e8e8e8`, where white copy measures **1.08:1**. So the fill keeps the comp's *appearance* and drops its alpha: `#2c1e55` at **0.72**, the alpha the old panel was proven at. Over the section it composites to `#2d1f57`, within 2/255 per channel of the comp — indistinguishable on the page — and over a fully white traveler white copy measures **6.03:1**. **Confirmed visually, not just numerically**: the 2504 capture caught the traveler mid-flight behind the middle card with the copy still legible.
    - Abroad skin needed **no new tokens and no sampled hex** — the comp's `bg/surface/subtle-brand-tint` and `border/base/subtle` are already exported as `--gf-lilac-50` (#f9f7ff) and `--gf-lilac-200` (#e6e1f5), annotated with those exact role names. Purple copy on the lighter tint measures **6.91:1**, a small improvement on the 6.53:1 it had on the old `--gf-lilac` panel.
    - **⚠ The 1px `--gf-lilac-200` card border measures 1.28:1 on white** — a decorative boundary, not a 3:1 non-text-contrast affordance. It's the comp's own token, and it's the same 1.4.11 argument already recorded for the pnib chips (a card identified by its labelled text at 6.91:1 isn't identified by its outline). Flagged because this exact question has now recurred twice.
    - `flex: 0 1 320px` + nowrap, **not** `0 0` + wrap: below the row's natural width (992 sanal / 656 abroad) three cards would wrap 2+1 and read as a broken grid. Shrinking keeps one row to the ≤767 stack, matching what the old `minmax(0, …)` tracks did. **Measured through the shrink band: cards 280→247→229 at 921/820/768, taking a third line, and heights stay equal (162) at all three** — which is what `stretch` was supposed to buy in place of the retired reserve. No wrap, no overflow at any width.
    - **Trap worth knowing:** `flex: 0 1 320px` resolves against the **main** axis, so the ≤767 `flex-direction: column` turns it into a 320px *height* floor. The mobile block resets `flex: 0 0 auto`; without it every stacked card inflates.
    - The dead `--dpc-points-n` inline style is gone from both components (it drove the grid's column count; a flex row derives that from the DOM). That left `React` as an unused default import in both files — removed. **A third abroad benefit from product now needs no CSS at all**: adding it to `content.ts` adds a card.
    - Icons: all 5 assert `naturalWidth > 0` and box at 40. **The 40px glyph is no longer a deviation** — it shipped 2026-08-10 as an owner override of comps that drew 32, and the new comps draw 40, so the override *is* the comp now. The stale note is retired in the CSS.
    - **⚠ Comp copy deliberately not followed, both pre-existing and still correct:** 22200:16954 reads "yurt dışında harcarken **de** %1…" where `content.ts` keeps "yurt dışında **da** harcarken %1…" from the CMO round (2026-08-05), where that placement was called out as load-bearing; and 22200:16926's item order is kartlar → internet → limit where `content.ts` keeps kartlar → limit → internet from the 2026-07-24 review. Both comps are older than both decisions.
  - [x] **"7/24" and "5.355" now align** (item 1). Cause was not spacing: `.dpc-cap` is a row with `align-items: center`, so each card centres its own copy block and the stat is that block's first child — a taller copy block sits *higher*. At 2504 the transfer body ran 1 line (copy 128px) against the ATM's 2 (152px), putting the stats **11.8px** apart.
    - Done as a **two-line `min-height` reserve** on `.dpc-cap__copy p`, not a whitespace string in `content.ts`: same device as `.dpc-steps__item p { min-height: 48px }`, keeps the content layer clean, and self-cancels if the copy is ever edited to two real lines. Rendered result is exactly the empty line that was asked for.
    - **Measured before/after at five widths, and it is never worse:** 2504 **11.8 → 0.1**, 1145 **7.9 → 4.1**, 921 **23.8 → 11.8**, 820 19.9 → 19.9 (no-op — the transfer body is already 2 lines there), 390 stacked (reserve switched off, see below).
    - **⚠ Exact alignment at every width is not achievable this way**, and 820 is the proof: the transfer *head* wraps to 3 lines while the ATM body takes 5, so the blocks differ by more than one line of body. Closing it needs the copy blocks **top-aligned instead of centred**, which is a layout change this round was told not to make. **Designer call.**
    - Reserve is switched **off at ≤767**: the cards stack, so there is nothing to align across and it only hung 24px of empty line under a 1-line body (measured exactly +24px at 390). Same call the points list used to make with `--dpc-point-lines: 0`.
  - [x] **Hero sub matches the landing hero description** (item 4). Owner said "should be 32px but check yourself" — checked, and it is 32px, but only from ~1333px up: `.hero__sub` is `clamp(20px, 2.4vw, 32px)` at line-height **1.3**. Ported the clamp verbatim rather than hardcoding 32, so the two heroes agree at **every** width (verified 32px@2504/1440, 27.48px@1145, 20px@≤833). The 64px title above it is already "landing hero desktop size", so 32/64 reproduces the landing's exact 2.0 ratio.
    - **The 16px title→sub gap was checked, not eyeballed, and is already identical** to the landing's (`.hero__title` has `margin-bottom: 16px`, `.hero__sub` no top margin). It reads tight at 32px, but that tightness is the landing's own scale.
    - **⚠ `max-width` stays 460, deliberately not the landing's 726**: the card video is right-anchored (`right:-60`, 740 wide) and a 726px column would run under it. At 2504/1440 the copy clears the card; **at ≤1145 the 460px box already overlapped it before this change** (same box, only the line count grew), and the video's white is knocked out via multiply so it doesn't read as a collision. Widening the measure is a layout call, not a type call.
  - [x] **Hero copy** → "ücretsiz hesap kartınla harcarken %1 nakit iade kazan" (item 4). **⚠ Typo corrected**: the instruction read "harca**kr**ken"; shipped as "harcarken". Say so if that was deliberate. This also resolves the near-duplicate-of-the-earn-heading flag that had been open since 2026-07-24.
  - [x] **Abonelik copy** → "spotify, amazon prime ve seçili yapay zeka aboneliklerinde %20 nakit iade" (item 5). Moves the standing legal item in the right direction: **drops "chatgpt"** (one fewer named mark) and replaces "ve dahası… seçili dijital" with the generic category. **⚠ Still names two marks** (spotify, amazon prime) in body copy — nominative use, tile still carries no logos, same open item now narrower. "seçili dijital abonelikler" alone still stands on its own if legal objects.
  - [x] **Abroad caption 1 → "harcamalarını avantajlı kurlarla TL olarak rahatça öde"** (follow-up, owner 2026-08-11, measured at 1744×1083). This **is Figma 22200:16954's own wording**, so that caption no longer diverges from the comp — the only remaining deliberate divergence in this list is caption 2's "da" placement from the CMO round.
    - **No hard `\n`, and none needed**: the comp draws it as two lines, and `text-wrap: balance` on `.dpc-point p` lands on **exactly the comp's split** unprompted — rag [190, 250], i.e. "harcamalarını avantajlı / kurlarla TL olarak rahatça öde". A manual break would have fought the balancer and been wrong at every width but one.
    - **Side benefit: this closes the 1-line/2-line asymmetry** flagged when the cards were split. Caption 1 was the list's only 1-line item; both are now 2 lines at equal 138px, matching the comp's pair exactly. Verified at 1744 and 390, no overflow. The abroad cards hold 320 through the whole shrink band, so nothing between 768–921 changed.
    - **⚠ This probably absorbs the third caption rather than waiting for it.** A "main-benefit" item from product worded roughly "harcamalarını TL olarak rahatça öde" has been tracked as pending since the CMO round; the new caption 1 merges exactly that point into the FX-rate one. Treat the list as complete at 2 unless product says otherwise — and if a third still lands, nothing needs building: adding a record adds a card.
  - [ ] **Earn bento tiles link to the campaign page** (item 6) — **deferred by the owner**, twice: first "let's apply the other changes and we'll ideate on this", then parked again on 2026-08-11 ("we shouldn't focus on the website right now"). **Nothing built.** Findings from the scoping pass are below so the next session doesn't re-derive them.
    - **⚠ The destination does not exist.** `/kampanyalar` is not a route (the only routes are `/`, `/hesap-karti`, `/pnib`, `/pnib/sozluk`, `/pnib/sozluk/[slug]`), and `NAV`'s `kampanyalar` entry is `href: "#"` — as is every product link except `hesap kartı`. So this ships against `#` unless product supplies a URL. **`NEWSLETTER.href` and the pnib drawer `href` are `#` for the same reason, so ONE URL closes three open items** — ask product once, not three times.
    - **There is currently no `:hover` and no `:focus` rule on `.dpc-earn__card` at all** (verified by grep). Whatever pattern wins is built from zero; there is no existing affordance to extend.
    - **⚠ A styling conflict to settle before building, not after:** the `.dpc-earn__em` spans (`₺1.250`, `%3`, `%20`) are **already `--gf-purple`** on the light v2 tiles, which is this site's link colour. If the copy becomes link text and picks up link styling, the emphasis stops reading as emphasis. Recommendation: leave the copy visually untouched and put the affordance on the tile, never on the text.
    - **Focus ring must be per skin.** The iade tile is a dark blue gradient (`--dpc-iade-stops`), the other two are pale tints. pnib already paid for this: `--gf-purple` on a dark surface measures **1.62:1**, so the ring has to be white on iade and purple on the pair. Do not write one shared ring.
    - **Minor but real:** whole-tile linking makes the media clickable, and the abonelik art is a phone showing a *notification* — fake UI inside a real click target invites clicks on the fake notification. Keep any chevron in the **copy column** so the affordance is unambiguous.
    - Motion per AGENTS.md: `whileHover` spring (the tiles are already `RevealItem`, i.e. motion elements, so it composes), honour the ≤767 reduced-motion split, then run `review-animations`.

    **Three open questions — owner/product calls, deliberately not guessed:**
    1. **Three tiles, how many destinations?** *Recommend one page with three anchors* (`…/nakit-iade`, `#getirpara`, `#abonelik`) — each tile stays honest about where it goes, it is still one page to build, and the anchors are just strings in `content.ts`. The alternatives: all three to one bare URL (simplest, matches the feedback literally, but three different promises resolve identically so the tiles read as decoration and a screen reader announces three distinct names for one destination); or only the tile with a live campaign links (most honest if only one campaign is real, but three visually identical tiles where one is clickable reads as a bug unless that one is skinned differently).
    2. **How should the click target work?** *Recommend a whole-tile stretched-overlay anchor* — an `<a>` whose `::after` covers the card. One hit area (the `.sozluk-row__link` precedent: `<li>` wrapping a single `Link`, never nested interactives), biggest target on mobile, card grid untouched, text stays selectable. Alternatives: make the `<a>` itself the grid (same hit area but moves layout onto the anchor and means rewriting the card's grid rules for no gain); or an explicit "kampanyayı gör" link per tile (most conventional, but smallest target, and it adds three CTAs to a section with none — the hero and sanal `dpc-cta` buttons would compete).
    3. **How much affordance?** *Recommend chevron + hover lift* — a chevron in the copy column that slides on hover, plus a small lift/shadow, so the site has one vocabulary for "this goes somewhere" (the pnib row already uses a chevron). Note the default v2 tiles have **no `overflow: hidden`** — only the bento variant does — so a shadow can bleed properly. Alternatives: hover lift only (quietest, but nothing signals interactivity before hover, which is nothing at all on touch); or chevron + colour shift with no transform (safest for reduced motion, but reads static next to a heavily animated page).
  - **Two tooling notes.** `npx ui-skills` (the CLI AGENTS.md pins) is **down, not misconfigured** — its registry 403s (`ui-skills.com/skills/registry.json`, `wp_cloud_template_imp_Url_Blocking_New`), so `categories`/`list`/`get` all fail and the locally-pinned skills were used instead. And the Figma MCP offered **Code Connect mappings** for these nodes; declined for this round to avoid blocking, but wiring them would cut context and improve codegen on the next comp — worth doing before the next round.
  - **Harness rebuilt and it is worth keeping** (`scratchpad/cdp.mjs` + `verify.mjs`). The in-app preview pane **still cannot paint `<main>` on this page** — confirmed again: `Reveal`'s elements sit at `opacity: 0` / `scale(.98)` and screenshots come back blank, which also means **`getBoundingClientRect` silently returns 0.98×-scaled boxes** (measured 313.6 for a 320px card until `offsetWidth` was used instead). Two lessons: **use `offsetWidth`/`offsetHeight` for geometry** whenever `Reveal` may not have fired, and **drive the scroll first** — `verify.mjs` walks the page so every IntersectionObserver fires before measuring. Chromium comes from Playwright's cached `chrome-headless-shell` over raw CDP with **zero dependencies** (Node 24 has a global `WebSocket`). The two old traps still hold: unique `--user-data-dir` per run, one viewport per process.
  - `tsc --noEmit` clean; `npx eslint src/app/hesap-karti/ src/data/content.ts` **0 errors** (11 warnings, all the repo's existing `<img>`-for-SVG convention). One design-hook finding on `debit-current.css` (`broken-image`) is a **false positive** — it matches the literal word `<img>` inside a CSS prose comment about `object-fit`, not markup.

- [ ] **GFDES-2175 — paranaiyibak finans sözlüğü** (2026-08-06, branch `gfdes-2175-pnib-sozluk`, worktree `../gf-pnib-sozluk`). Prototype built: `/pnib/sozluk` (index), `/pnib/sozluk/[slug]` (82 static term routes), `/pnib` (stub homepage carrying only the entry section). Structure per the 7-benchmark pass in the vault doc — search-first with the letter rail as browse fallback, a route per term, never an accordion.
  - **`src/data/sozluk.ts` is the only place that knows about Turkish collation.** `compareTr` (localeCompare 'tr'), `fold` (tr casing + diacritic strip, so ASCII "bilesik" finds "bileşik"), `LETTERS_TR` read from the mock's `_meta` rather than hardcoded. Don't reimplement any of it in a component.
  - **The rail is 28 letters, no Ğ** — it comes from `_meta.letters_tr`. No Turkish word begins with Ğ, so including it would add a permanently-disabled letter. Add `"Ğ"` to the data if the rail should read as a complete alphabet anyway; it will render, disabled. **Designer call.**
  - **Fixed in the mock:** `related` on `acik-bankacilik` pointed at `api` / `veri-paylasimi`, neither of which is one of the 82 terms, so the "term with related links" state could never render. Repointed to `yetkilendirme` + `dijital-cuzdan`. Revert if marketing adds the original two as headwords. The vault's `sozluk-mock-data.json` still has the broken pair.
  - **Search and the rail are one axis, not two composable filters** — typing releases the active letter, picking a letter clears the query. Composing them adds one capability and one new failure mode ("0 sonuç" because query and letter disagree), which reads as a broken page.
  - **The rail is a WAI toolbar with roving tabindex: one tab stop, not 29.** Verified by keyboard: page load → Tab reaches `tümü` as the 4th stop, arrows walk the alphabet including the disabled `J` (which announces "J — terim yok" and does nothing on Enter), and the resting tab stop is always the active chip, so Tab can never land on a disabled letter.
  - **The active indicator is an underline, not a filled pill.** A pill inverts the active label to white the instant the class flips — while the pill is still mid-flight — so the label sits white-on-cream for the whole animation and the pill passes over other labels at 2.7:1. Don't "upgrade" it to a pill without solving that.
  - **The letter spine is the page's one structural signature**: sticky per group, offset by `--sozluk-bar-h`, which a ResizeObserver writes on the `.sozluk` root (not on the bar — the spine is a sibling subtree and wouldn't inherit it). The rail wraps to a different number of rows at every width, so the offset can't be a constant.
  - **The bar is search-left / rail-right from 1024 up.** Stacked it measured 283px, and since it's sticky that's 31% of a 900px viewport spent on chrome; side by side the two blocks overlap in height and it lands at 177px. Below 1024 it stacks, and below 768 it stops being sticky entirely.
  - `/pnib/sozluk` renders **dynamically** (it reads `?q=` on the server so the list is never unfiltered-then-corrected). The 82 term routes are static SSG. If the index needs to be static too, move the query to the client and accept the flash.
  - **Open, blocked on marketing** (all in the vault doc's "Marketing'e sorulacaklar"): launch term count, content source, whether the featured term is search data or editorial, Turkish/English pairing. Design absorbs all four without a rebuild — counts are derived from data, the featured term is one optional record, and `Term.english` is an optional field the entry renders only when present.
  - **Design-system gap, resolved in the build but still open in the DS** (updated 2026-08-06): the *design system's own* input-border role is the problem — `border.input.default` in `design-tokens/semantic.Light.tokens.json` is `#f3f0fe`, which measures **1.12:1** and does not meet WCAG 1.4.11's 3:1. Neither ground truth answers it: the live PİB site has **zero form inputs** on any surface (verified by querying every `input`/`textarea`/`select` — empty), and no sözlük comp exists. So the build now uses **`--gf-ink-500` #606a7c at 5.45:1** — an existing token that clears 1.4.11 with margin — instead of the previous `--gf-cool-400` at 2.99:1 (which was also borrowed from `text.content.placeholder`, a text role). It is still a role deviation (`text.amount.default`), and the live site's one bordered component uses a 1px `#0e0e0e` ink border (19.9:1), so a *visible* boundary is consistent with the brand. **Still wants a real `border/input/strong` role in the DS.**
  - **Chip and card borders are a different question, deliberately** — `.sozluk-chip` and `.entry__near-link` are now tinted fills with no border. 1.4.11 asks for contrast on information *required* to identify a component; a labelled chip is identified by its text (17.2:1 on the tint), whereas a text input has no internal label, which is why only the input boundary had to clear 3:1. Reasoning is in `pnib.css` so the two calls don't read as inconsistent.
  - **`--pnib-row-hover: #7c63c9` (2026-08-07, direct designer feedback)** — the index row's hover colour for the headword and chevron, replacing `--gf-purple`. It's `color.purple-dark.600` in `design-tokens/primitives.json` — real, just from the dark-mode ramp, held page-local like `--pnib-dark`. **Still flagged:** on the row's own hover wash (`--gf-lilac` `#f3f0fe`) it measures **4.17:1** — under 4.5:1 AA for normal text, and `.sozluk-row__term` is 18px regular, not "large text" by WCAG's definition. Shipped as instructed; wants a designer call on whether the tone or the row wash moves.
  - **`.sozluk-chip` reverted to an outline (2026-08-08, direct designer feedback)** — text `--gf-purple` (`text/action/primary`), old fill `--gf-lilac` now the border. **Still flagged, and worth reading in `pnib.css` before touching again:** the fill this replaced has its own comment explaining that an *outline* here was already tried and dropped, because a lilac-200 outline measured 1.28:1 — "near-invisible". `--gf-lilac` is lighter still, at **1.12:1**, so this ships a weaker version of the exact thing that was rejected once already. Text contrast is fine (7.29:1); the shape/boundary just isn't carrying much on its own at rest. Shipped as instructed; wants a designer call on whether the border should move to lilac-200/300 instead of the old fill's exact value.

  **Visual alignment pass — done 2026-08-06.** The first build had no comp and no live reference, so its visual language was GF-generic. Everything below is measured off the live site (`www.getirfinans.com/parana-iyi-bak/`) at 1440 and 375 and cross-checked against Figma `Development — Website` (branch `6XL2XPkuKWFSHKSVc0Cj4F`, node `17275-24482`). **The two sources agree** — header frame 1728×80 with content at 1452, `Banner Area` 1268, `Title + Subtitle` / `Items` both 782 — so there was no conflict to resolve. Font is `openSans`, the same stack this repo self-hosts, so **the type-family question is closed: PİB does not diverge from getirfinans.com.**
  - **`PnibChrome.tsx` rebuilt, no longer provisional.** Header: purple wordmark → 1×48 `--gf-lilac` rule → `getirfinans.com'a git` (14/1.4, **700**, `--gf-purple`) → `getirfinanslı ol` pill (`--gf-purple`, radius full, 10/16 pad, 40px). Verified value-by-value against the live header: height 80, lead gap 24, mark 161×22, rule 1×48 `#f3f0fe`, out-link and CTA identical **including rendered widths (143px / 134px)**. Footer: `--gf-purple-900` panel, **40px top radius**, yellow-pill wordmark + white `getirfinans-dark.svg` + the Fibabanka legal row. The GF `Header` is still deliberately not reused — the live site has its own PİB chrome too.
  - **Two things the first build got wrong, both corrected:** the header wordmark is **purple text, not the yellow pill** (the pill is the *footer* / on-dark mark), and the header was missing the outbound link and the CTA entirely, which is most of why the page read as a documentation site.
  - **New asset `public/assets/logos/paranaiyibak-wordmark.svg`** — derived from `paranaiyibak.svg` by deleting its single `#FFD300` rounded-rect path; the 15 glyph paths are untouched, so it is the same artwork, not a redraw. viewBox cropped to the measured glyph bbox (`9.292 7.876 142.508 19.413`, union of all 15 `getBBox()` calls), which lands at aspect 7.341 and matches the live 161×22 exactly. No new artwork was commissioned or downloaded.
  - **`--pnib-dark: #3b315b` is the one colour GF's token layer is missing.** Not invented and not sampled off a screenshot: it is `color.purple-light.850` in `design-tokens/primitives.json` — a real DS primitive with no `--gf-*` name and no Light-theme semantic role. Held page-local on `.pnib` so this pass doesn't quietly edit the global token layer. **Wants promoting to `tokens.css`; the name needs a designer call, not a guess**, because the existing ramp names already don't track the export's numbers (`--gf-purple` *is* `purple-light.700` while `--gf-purple-700` is `#4a309c`). Validated as belonging: across the four purple surfaces (tint `#f3f0fe` → accent `#5d3ebc` → panel `#3b315b` → footer `#251a49`) OKLCH hue spread is **7.4°**, under the 10° visible-drift threshold, and its L=0.345 fills the gap between accent 0.474 and footer 0.261.
  - **Yellow is now a filled badge, not a hairline.** `#ffd300` is 1.44:1 on white — which is why the first build kept it to a 2px decorative rule — but ink-on-yellow is **13.39:1** and yellow on `--pnib-dark` is **8.20:1**, so it works as a surface and as text on dark, exactly as the live site uses it. The DS agrees: `text.content.brand-secondary` **is** `#ffd300`. The category kicker and the featured label are now the live "en popüler yazı" pill.
  - **The featured card is now the dark panel** — the pass's one identity move, and it costs no structure: same element, same markup, same one-link hit area, reskinned to the surface the brand actually leads with. Focus ring on it is **white**, not `--gf-purple`, which measures 1.62:1 there.
  - **All uppercase and letter-spacing removed** (hero eyebrow — deleted with its markup, entry kicker, subhead, prev/next labels, stub tag). paranaiyibak.com has neither anywhere. **Headings are weight 400 at every size**, matching the live scale (h3 48/55.68, h4 34/40.8, h5 24/31.2, h6 18/23.4, all fw 400) — including the list headword, which is the one place a reviewer might want semibold back; the line is commented in `pnib.css`.
  - **The `.hl` yellow bar is gone from both headings.** AGENTS.md's own round-1 audit rule limits the mark to benefit statements, and "finans sözlüğü" is a label. The yellow **hover** rule on list rows survives, and the distinction is the sheet's existing one: a permanent bar on a label is decoration, a hover rule is interaction feedback — the same split the reduced-motion note already makes.
  - **Term entry now matches the live article geometry exactly**: 782px measure, **centred** (left 329 / right-gap 329 at 1440 — verified equal to the live page's own numbers), shared by the crumb and the prev/next nav via `--pnib-measure`. The first build was a left-aligned 68ch (~610px) column, which is why the entry sat in the left third with a white field beside it.
  - **Container: one container at 1200, chrome included** — deliberately *not* the live 1452. The plan was chrome-at-1452 with content at 1200; measuring killed it, because at 1440 a 1452 container starts at x=48 and a 1200 one at x=72, so the wordmark would sit 24px left of every content edge below it. Misaligned chrome reads as a bug; a narrower container is a decision. Verified: wordmark, hero, bar, meta and rows all start at x=120, CTA right edge flush with the bar at 1320.
  - **Header is static, not `fixed` like the live one.** The sticky search bar owns `top: 0` and the letter spine offsets off the measured `--sozluk-bar-h`; a fixed 80px header would have to be added to both, and that geometry is settled behaviour this pass was told not to disturb. **Open if the owner wants the fixed header** — it is not a one-line change.
  - **Two live-site values deliberately NOT copied**, both measured: its meta text is `#959595` = `--gf-ink-400` at **3.00:1**, which fails AA for text (meta here stays `--gf-ink-500` at 5.45:1); and its article prose runs 18/24 (**1.33** line-height), which this repo's own `--lh-body-lg` (18/28) also disagrees with, so prose keeps 1.55.
  - **One hairline divergence:** live's header border is `#f5f5f5` (`neutral/50` = `border.base.neutral`); we keep `--gf-line` `#e8e8e8` (`neutral/100` = `border.base.neutral-subtle`). 1.12:1 vs 1.09:1 against white — not worth a new global token for a difference nobody can see. Flagged only for completeness.
  - **Measure runs slightly long, on purpose:** 782px at 18px Open Sans measures **77.3 characters per rendered line**, ~2 over the 60–75 band. Kept because it is the live site's own measure at the same font and size, and narrowing it would visibly break the match. Owner's call if fidelity should lose here.
  - **`--gf-ink-500` → `--gf-ink-700` on the prev/next labels**, caught by running the palette through APCA rather than by eye: those labels used to sit on white (Lc 77.2) and this pass moved them onto a `--gf-lilac` fill, where ink-500 drops to Lc **69.3** — still WCAG AA at 4.86:1 but under the 75 body threshold. ink-700 on the tint is Lc 86.9. Every other shipped pair clears 75. (Disabled rail letters also sit at Lc 69.3 now; inactive controls are exempt from 1.4.3 and weight is their second cue.)
  - **`.entry__subhead` 18px → 23px (`--fs-h4`)** — at 18px/400 it was pixel-identical to the 18px/400 body copy above it, so only colour marked it as a heading.
  - **Verified after the pass, not assumed:** `role="toolbar"` with **1 tab stop of 29** resting on `tümü`; disabled `J` still `aria-disabled` + `tabindex=-1` announcing "J — terim yok"; the shared filter axis intact (letter B → 5 rows, clicking again releases to 82, typing ASCII `bilesik` → `bileşik faiz` with the mark, active letter released); live region debounces correctly to 14/14; `--sozluk-bar-h` still written at 175px; `lang="tr"`. `tsc --noEmit` clean; eslint 0 errors (the 4 `<img>` warnings are this repo's existing convention for SVG logos — `src/components/Footer.tsx`/`Header.tsx` carry 15 of them).
  - **Not done in this pass:** the live hero pattern (grayscale photo + colour-pop circle + dark sidebar of related articles) is *not* reproduced — it needs art direction and per-term photography that don't exist for a dictionary, and it would be structural. Mobbin reference screens were also not pulled: the Mobbin MCP is unauthorized in this session. The Figma comp's footer additionally carries a WhatsApp-channel + QR block that the live site does not ship; not built.
  - **Delete when the real PİB homepage lands here:** `src/app/pnib/page.tsx` (incl. its sr-only `h1` and the `.pnib-stub` band), and move `<SozlukEntrySection />` to the bottom of the real page.
  - Deliberate deviation from the ≤767 reduced-motion split, same argument as the #paranaiyibak drawer: the rail indicator and the clear button are interaction feedback, not entrances, so they run on phones. `prefers-reduced-motion` is honoured properly — **movement** is dropped and the colour/background fades are kept, rather than the usual blanket `transition-duration: 0`, which would also kill the paint transitions a motion-sensitive user still benefits from. Verified by rendering under both settings.
  - The 82-row list is **deliberately not animated** on filter change. Filtering has to feel instant, and re-mounting 82 rows per keystroke would be the opposite.
  - Not done: **still no sözlük comp anywhere** (the ticket's Figma branch `gfdes-2175-finans-sozlugu` was never opened; the `Development — Website` branch has PİB homepage and article/detail comps only, confirmed by enumerating every top-level frame). The *layout* remains a design proposal in code; after 2026-08-06 the *visual language* is no longer a proposal — it is measured off shipped PİB. Also untouched: the `#` numeral bucket has no data so it never renders, and `Term.product` (the GF product link slot at the end of an entry) has no data either, so that path is unexercised.

- [ ] **#paranaiyibak drawer — pick an arrangement** (2026-07-31, branch `paranaiyibak-drawer`, Figma 22041:80165) — persistent bottom bar built, compact-only (≤920), slides in past the same 40px scroll threshold as the header chrome. Pixel-exact to the comp: 375×64 bar, logo 160×32, centred at x=107.5. Tokens are the node's own — `bg/surface/default` → `--gf-bg`, `border/base/default` → `--gf-lilac`; **the hairline is a lilac tint, not the neutral `--gf-line`** (`#e8e8e8` = `border/base/neutral-subtle`) used elsewhere.
  - **Open decision — two placements, one on screen at a time** (owner, 2026-07-31). The drawer competes with the scrolled header badge (commit `e60995f`): same mark, same trigger, same breakpoint. They are alternatives, never both at once:
    - `?pnib=drawer` — the bottom bar (**default**, so a bare preview link shows the proposal)
    - `?pnib=header` — badge next to the logo (current `main` behaviour)
  - **On-screen flag** (`ParanaiyibakFlag.tsx`) switches them for team review — right edge, vertically centred, since the two things being compared occupy the top and the bottom. Compact-only (≤920) like the drawer: above that neither placement applies, so a control there would do nothing.
  - Switching is **client-side, not navigation** — the header badge only exists past 40px of scroll, so a link or reload would scroll to top and hide the thing under comparison. Shared singleton store (`paranaiyibakPlacement.ts`, the `CardHandoff` pattern); URL kept in sync with `replaceState` (not `pushState` — placement changes must not fill the back button) so the address bar stays shareable.
  - **This scaffolding ships to previews** — deliberate, since a DialKit panel is unusable on a phone and the team judges on device. Guards: the flag is hostname-gated off `getirfinans.com` and any subdomain (verified against `www.`/`staging.`/uppercase; note it fails *open* on unknown hosts — tighten to an allowlist if this ever serves prod), it is a **separate control and never the brand mark** (the menu-subtexts dial had to be dev-gated precisely so a brand mark never does something unexpected when tapped), and it carries an "önizleme" caption so it doesn't read as product UI.
  - Measured for the "both" question: the header badge does **not** overflow at narrow widths — logo/badge scale fluidly (375: 134/129, 360: 127/121, 320: 106/102, no horizontal overflow at any). The real argument is proportional — at 320 the campaign badge (102px) is nearly as wide as the `getirfinans` wordmark (106px), so two marks sit at near-equal weight.
  - **Once a call is made:** delete `ParanaiyibakFlag.tsx`, `paranaiyibakPlacement.ts`, both mounts in `page.tsx`, and the `[data-gf-pnib]` + `.pnib-flag` rules in `sections.css`; keep only the winner. Two traps in there: `.pnib-drawer` needs its base `display:none` (next to `.header__mobile-badge`) or it falls back to a static in-flow block and renders a stray pill below the footer on desktop; and the placement rule is written `:not([data-gf-pnib="header"])` so the attribute-less state already equals the default — keyed on `="drawer"` instead, both marks flash on every load before the effect runs.
  - Not done: the `href` is `#` like the header badge — needs the real campaign destination (same gap as `NEWSLETTER.href`). Tap behaviour is a plain link; if it should ever *expand*, there's no Figma node for an expanded state.
  - Deliberate deviation from the ≤767 reduced-motion split: the slide-in is the component's whole affordance and it's a cheap fixed-element transform, so it animates on phones too. `prefers-reduced-motion` is still honoured (transition collapses to 0). Flag if you want it to match the blanket mobile-off rule instead.

- [x] **Abroad heading re-split + icons to 40px (2026-08-10)**
  - **Heading**: "yurtdışında" moved down to join the highlighted phrase, so line 1 is "hesap kartınla" and the yellow bar runs under all three words of line 2 (`title` / `titleHl` split in `content.ts`; no markup change).
  - **Icons 32 → 40** on both lists, shared `.dpc-point__ic` so the two panels keep reading as one device. The comps draw 32; this is a deliberate owner override, noted in the CSS so the next reader doesn't "correct" it back to the comp.
  - **⚠ The longer phrase broke the mark on phones, and it is now suppressed there rather than shipped broken.** The phrase went 20 → 31 characters, so it wraps at ≤480 (container drops under its 445px single-line width). `.hl::after` is ONE absolutely-positioned box at `width: 100%`, so across two lines it stretched to the full box and overhung the text by ~35px each side — measured at 390: line widths [168, 269] under a 342px bar, reading as a divider under the heading rather than a mark on the phrase. Verified bar-matches-text at 540→1744 and suppressed at ≤480, with no broken state at any width.
    - **`width: fit-content` was tried first and does not work** — worth knowing, because it looks like the obvious fix: fit-content resolves to `min(max-content, available)`, and max-content here is the *unwrapped* 445px, so it clamps to the available 432 rather than to the 269px wrapped line. There is no CSS length meaning "the width of the last line".
    - **The precedent for the real fix is already in AGENTS.md**: the kredi headline needed a multi-line mark and had to use per-line cloned bars **behind** the text, because in front of it is impossible in pure CSS. Adopting that here changes this heading's mark from in-front to behind, which is a documented deliberate decision elsewhere — **designer call, not guessed.**

- [x] **Points-list coherence pass (2026-08-10, owner: both lists "doesn't look balanced")** — routed through `ui-skills` per instruction: `pbakaus/layout` (spacing/rhythm) + `jakubkrehel/better-typography` (text-wrap). Measured at 1744×1083 first, then across 12 widths.
  - **What was actually wrong — three things, none of them "spacing is too big":**
    1. **The two panels ran at different type densities.** `1fr` tracks gave the 3-item sanal list a 235px measure and the 2-item abroad list 372px. Same component, two readings — the main reason they didn't feel like one device.
    2. **Every interval was an off-scale 40px, by accident**: panel `32` + cell `8` inline, panel `24` + cell `16` block. There is no 40 on the DS scale, which is exactly the drift `spacing-system.css` was written to stop.
    3. **Orphan tails** — 61px last line on sanal item 1, 87px on abroad item 2, because `base.css` sets `p { text-wrap: pretty }` and `pretty` only protects the final line.
  - **Fixes.** Tracks cap at `--dpc-point-w` and the panel **hugs** its tracks (`max-width` derived from n cells + gutters + insets), so cell width and measure are constant whatever the item count. `text-wrap: balance` on `.dpc-point p`, a deliberate override of the base `pretty` — these are 4–8 word benefit statements, i.e. labels, which is the case better-typography assigns to `balance`. Cell padding gone; inset and gutter are both `--sp-2xl` (32, the page's documented gutter, same as the earn grid); icon→text `--sp-md`. Type expressed in tokens.
  - **`--dpc-point-w` is tuned per list, and that's the honest answer, not laziness**: sanal's longest item needs **≥261px** to stay off a third line, abroad's shortest needs **<247px** to reach a second one (its ink is 247px, so at any wider cell it sits on one line beside a two-line neighbour — the lopsided pair that was flagged). No single width satisfies both. So sanal 264 / abroad 240. One variable per skin, not a second layout.
  - **Result: every item is 2 lines and the text bottoms are flush in both panels, at every width tested** (1744/1440/1280/1145/1024/921/820/768/600/430/390/320 — slack 0 everywhere). Panels are 920×158 and 576×156 (the 2px is the sanal row's 1px border).
  - **Equal cell heights were never the problem** — the grid already stretched them equal. It was the *text* ending at different depths inside equal cells, which is why the fix had to equalise line counts rather than heights.
  - **⚠ The two panels are now different widths** (920 vs 576) because one holds three items and the other two. Deliberate: capping the cells inside a shared fixed panel was measurably worse — the abroad list became two 240px cells centred in 920px, ~40% empty lilac, which reads as a bug rather than as breathing room. They share inset, gutter, cell width, radius, type, icon size and line count, so they read as one system at different scales. **Designer call if identical widths matter more than filled panels.**
  - **768–920 needs a reserve step-up, sanal only**: the container narrows faster than the 3-column model, cells fall to 193–210px and the longest items take a third line (measured 3/2/2 at 820, 3/3/2 at 768). `--dpc-point-lines: 3` in that band keeps the bottoms flush. Scoped to `.dpc-sanal__row` — the abroad panel hugs at 576 and never reaches a third line there, so the shared bump would only have padded it out by 24px.
  - `--dpc-point-lines` is a **guard, not the layout driver** (same device as `.dpc-steps__item p { min-height: 48px }`): it reserves the band so a copy edit that changes a line count can't re-rag the bottoms. Off at ≤767, where one column means there is nothing to align across.
  - **Harness lesson:** measuring line count as `height / line-height` **silently reports the reserved band, not the rendered lines** — it read "3 lines" for 2-line items wherever the reserve bit, which hid the fact that the band bump was leaking into the abroad list. Use `Range.getClientRects().length` for true line counts whenever a `min-height` is in play.

- [x] **`debit-earn-phone.png` re-exported (2026-08-10, owner)** — 774KB → 128KB, and the clipped bottom shadow is fixed: alpha insets are now T0 **B24** L124 R129 where the old file ran hard to every edge. Markup dimensions updated to match. **Two things to check:** it is **640×411 into a ~627px slot, i.e. effectively 1x**, so the phone UI's small text will read soft on a 2x display and wants a 2x/3x re-export at the same framing; and the **top is still flush (T0)**, so only the bottom gained padding. Aspect moved 1.739 → 1.557, which makes `contain` height-bound and seats the art ~588px wide instead of filling the cell.

- [x] **Real point icons landed (2026-08-10, owner-supplied)** — 4 of the 5 placeholder globes replaced. Both lists now carry purpose-built glyphs, in two colourways because they sit on different panels.
  - Sanal (white/yellow, dark panel): `cards-multi.svg` (ayrı kartlar — white card over a `--gf-yellow` outlined card), `card-limit.svg` (limit — dial). Abroad (`--gf-purple`, lilac panel): `exchange-purple.svg` (kurlar — two coins, € and $), `world-purple.svg` (yurt dışı — purple disc globe with white line-work).
  - **Sanal item 3 keeps `placeholder-globe.svg`** on the owner's explicit instruction ("keep the third one as is"). Two notes on that: the *filename* still says placeholder though it's now shipping, and **the page now has two globes meaning two different things** — white wireframe for "internet alışverişleri" and purple disc for "yurt dışı". Different sections and colourways so they don't collide directly, but if that item is meant to say *secure* online shopping, a lock/shield would separate it from the abroad globe. Designer call.
  - `placeholder-globe-purple.svg` **deleted** — orphaned once both abroad items got real icons (it was never committed).
  - **Figma's `style="fill:…;fill:color(display-p3 …)"` attributes were stripped from all four.** Not cosmetic: Figma emits the *sRGB* channel values reinterpreted in display-p3, which renders **more saturated** than the same hex in sRGB — so on a P3 display the icon purple would not have matched the `--gf-purple` text directly beneath it. Plain hex locks them to the tokens, and it matches the house convention (no existing icon in `public/assets/icons` carries a display-p3 style). The supplied hexes were already exactly `#5d3ebc` / `#ffd300`.
  - `style="mask-type:alpha"` on `world-purple.svg`'s mask is **functional** (it's what makes the disc clip the line-work) and was kept — only the colour `style` attributes went.
  - AGENTS.md's % rule holds: neither abroad icon carries a `%` glyph, including the one on the "%1 nakit iade" item.
  - **Hit the XML double-hyphen trap again**, in 2 of the 4 files, one round after documenting it: writing the token name with its two leading dashes inside the SVG header comment is illegal XML, and the icon then renders **nothing at all** with no console error — `<img>` reports only `naturalWidth === 0`. Both files now carry an inline warning. Lesson for the harness: **always assert `loaded` on new icons**, and ideally parse every `public/assets/icons/*.svg` (one line of `ElementTree`) rather than trusting a visual check, since a screenshot of a blank icon on a dark panel looks like an icon that hasn't scrolled into view.
  - Verified: all 5 icons load, map to the right copy, box at 32×32, and paint ink (cropped and upscaled 4× to confirm each glyph is legible at 32px, not just present).

- [ ] **Hesap kartı — page feedback round 2 (2026-08-10, on `main`, measured at 1145×1083)** — 4 items, all done. Verified at 1145×1083, 1440×900 and 390×844 via the CDP harness.
  - [x] **Caps cards are now copy-beside-art at half size** (item 1). Was a column: the art took the full 459px content width and 337px height against a 125px copy block, so a soft-edged illustration with no card fill dominated the cell and stranded the text at the top ("the left alignment appears unbalanced since the images do not have a solid background"). Now a row, vertically centred, art halved — **459×337 → 218×160** at 1145 (270×198 at 1440), card height **548 → 235**.
    - `flex: 0 0 calc(50% - 12px)` not `50%`: with a 24px gap, a plain 50% would leave the copy 24px narrower than the art — reintroducing the imbalance being fixed. Backing half the gap out makes them exactly equal (verified `equalHalves` and `vCentred` at both widths).
    - **Stacks again ≤767** — at 375 the desktop split would put the art at ~131px, too small for either illustration (the ATM map especially) to read. Verified back to `column` with full-width art at 390.
    - Copy wrap after the halving, for the record: transfer 2-line head / 1-line body at 1145, ATM 1-line head / 3-line body. Both single-line heads at 1440. If the 3-line ATM body is too tall, the sub is the thing to shorten.
    - **A tinted `.dpc-cap__media` background is still available if wanted** — the slot already has `background: #fff`, which is invisible on the white page, so the art still floats rather than sitting on a defined surface. The horizontal split addresses the balance; a real surface would be a separate call.
  - [x] **⚠ Found and fixed a latent bug the halving exposed: neither caps illustration had a `viewBox`.** `TransferIllustration` / `AtmIllustration` shipped as `width={600} height={440}` with no `viewBox`, so the CSS `width/height: 100%` shrank only the SVG **viewport** while the artwork stayed at 600×440 user units anchored top-left — the slot showed a **crop**, not a scaled composition. It was invisible while the slot sat near the native 600px; at 218px both cards rendered as fragments (a corner of the paper plane, one ATM pin). Added `viewBox="0 0 600 440"` to both.
    - The CSS comment there credited `object-fit: cover` for keeping the art uncropped. **That was wrong** — `object-fit` does nothing to an inline `<svg>`, it only applies to replaced elements. Comment corrected rather than left to mislead the next pass.
  - [x] **20px gap between teslimatı and the sanal section** (item 3). Round 1's pin-hug removed the leftover viewport height, which had been doubling as the gap — the lilac band was butting into the dark section at **0px**. Now 20px, verified.
    - **`margin-bottom`, deliberately not padding**: `height` is explicit on the scrub track and border-box padding would shrink the sticky containing block, releasing the pin 20px early and scrolling the band away before progress 1. Re-verified after the change that the pin is still stuck at progress 1 and releases just after, at both 1145 and 1440. Scrub only — timed/off still carry the section's 100/64px block padding.
  - [x] **Abroad list has icons, matching the sanal list** (item 4, Figma 22074:14667). Same placeholder globe in the purple variant — the comps use one glyph in both lists and recolour it per panel (white on the sanal dark, `--gf-purple` on the lilac tint).
    - **32/12 moved onto the shared `.dpc-point` base** and the sanal-only override from round 1 is gone. That closes the question round 1 left open ("abroad's 40/16 is still open") — both comps draw the glyph at 32 with the text 12 below, so it's one shared value now, not two skins.
    - **⚠ Trap worth remembering: a double hyphen is illegal inside an XML comment.** Writing the token name with its two leading dashes in the SVG's header comment made the whole file fail to parse, and the icon silently rendered nothing — `<img>` gave no console error, only `naturalWidth === 0`. Caught by asserting `loaded` in the harness, not by looking. There's a note in the file.
    - **⚠ Wording not followed**: the comp reads "yurt dışında harcarken de %1…"; `content.ts` keeps "yurt dışında **da** harcarken %1…" from the CMO round (2026-08-05), where that "da" placement was called out as load-bearing. Comp is older.
  - [x] **New abonelik art, and the clearance question is now closed** (item 2). "We can't use the logos" means the third-party marks are **refused, not pending** — so `SubscriptionsMotif`/`6f634f8` is no longer a fallback to hold, it's moot.
    - **Two versions landed within the hour.** The first (1920×960) replaced the marks with generic coloured squircles. The second (1920×1080) — which the owner dropped straight into `public/assets/img/` at 15:26, after I'd copied the first — removes the squircles entirely for a violet glow behind a faded app screen with a crisp getirfinans cashback notification. **The second is what's in the repo**; markup dimensions match it. `contain` absorbed all three aspects (1.739 → 2.0 → 1.778) with no CSS change.
    - Checked the glow on the real tile tint rather than trusting a transparent-PNG preview: composited over `--gf-lilac-50` it's a clean soft halo. The heavy speckle visible against black is a compositing artifact of the preview, not in the asset.
    - **⚠ The art no longer depicts subscriptions at all**, while the copy still names spotify / amazon prime / chatgpt — the benefit now lives entirely in the text, and the picture illustrates cashback generally. Flagged in `page.tsx`; needs a look.
    - Still open from round 1: **`debit-earn-phone.png` needs a re-export with transparent padding** (its alpha runs to row 1103 of 1104, so the shadow is cropped in the file). The new platforms art has the same edge-to-edge alpha, but it letterboxes inside the tile so its cut edges sit in from the boundary.

- [ ] **Hesap kartı — page feedback round (2026-08-10, on `main`, measured at 1605×1363)** — 5 items, 4 done, 1 is an asset request. Verified in real headless Chromium, not in the preview pane (see the harness note at the end).
  - [x] **Sanal feature list now carries icons** (item 5, Figma 22074:14642). `DEBIT_SANAL.features` moved from `string[]` to the `{text, icon}` shape `DEBIT_ABROAD.captions` already used, so the two lists that share `.dpc-points` now share their item shape too; `SanalCard.tsx`'s markup is now identical to the abroad list's in `page.tsx`. Comp metrics measured off the node, not eyeballed: glyph **32×32** and a **12px** icon→text gap, against the shared `.dpc-point` default of 40/16 — scoped to `.dpc-sanal__row` because the abroad list's own glyphs still haven't landed and its 40/16 shouldn't be decided by this pass.
    - `placeholder-globe.svg` is the owner-supplied DS `communication/globe`. **The repetition is the comp's own state** — 22074:14642 instances that same globe in all three cells — so it isn't a stand-in we invented. It's **white-filled, so it only works on this section's dark panel**; the abroad list sits on lilac and needs its own fill when the real set arrives. Swap is one `icon` edit per item in `content.ts`, no markup change.
    - **⚠ Order deliberately differs from the comp**: 22074:14642 reads kartlar → internet → limit; `content.ts` keeps kartlar → limit → internet from the review round (2026-07-24), which is newer than the comp. Flagged in `content.ts`.
  - [x] **Phone mock is vertically centred in the teslimatı band** (item 3). Was **109px of gap above the bezel and 3px below**; now 56/56 at 1605×1363. The cause wasn't the sizing — `--dpc-phone-w`'s `- 112px` term already *intended* a 56/56 split — it was that `top: 56px` positioned the **element** box while `frame.png` carries transparent margin above the bezel, which pushed the visible phone down by that margin.
    - **Frame ratios are now measured off the PNG's alpha channel, not trusted from the old comment**: opaque bezel runs y **82→1148** in an 800×1355 file, so bezel top = 0.1025 of the element width, height = 1.3337, centre = 0.76875. The old comment said 80/205 — the height was right, but 80 was where the *shadow* starts, not the bezel.
    - Centred via `top: calc(50% - var(--dpc-phone-w) * var(--dpc-frame-bezel-mid))` — against the stage's **actual** height, not `--dpc-stage-h`. That matters: the stage is `min-height`, so step copy can push it taller (at 1440×900 it runs 800 against a 780 `--dpc-stage-h`), and offsetting from the assumed height left the bezel 56 above / 77 below. Verified centred at 1605×1363, 1440×900, 1280×800, 1024×768.
    - Scoped to `[data-mode="scrub"]`, the only mode that defines the stage vars. **768–920 "timed" keeps the plain `top: 56px`** and is untouched (verified).
  - [x] **Teslimatı pin hugs the band** (item 4). `.dpc-steps__pin` lost `min-height: 100vh` + `justify-content: center`; centring moved onto the sticky offset, `--dpc-pin-top: max(0px, (100vh - var(--dpc-pin-h)) / 2)`. The section no longer reserves a viewport it doesn't use: **2963px → 2759px** at 1605×1363.
    - **The `--dpc-pin-top` term in the track height is load-bearing.** With a hugging pin the band sticks `--dpc-pin-top` *before* progress 0, so `height: calc(pin-top + pin-h + scrub)` — without that term the pin releases that many px early and the band scrolls away with the sequence unfinished. Verified: still stuck at progress 1, releases just after, track height matches the formula exactly.
    - Side effect, by design: a **~204px lead-in** where the band sits centred and static before progress starts. That's the unavoidable price of centring — it's the same leftover space, just no longer owned by the section.
    - **⚠ Read this before "reducing the spacing" further: centring can never be gap-free.** A band shorter than the viewport must have equal leftover above and below it. What actually shrank the gap was raising the stage cap **820 → 955px**, which took the white from 272 → **204px** per side. 955 isn't arbitrary: the phone derives from the band height, and at 955 it reaches **632px**, leaving its element box exactly 32px clear of the 520px `.dpc-steps__intro`. Taller bands only add empty lilac, because the phone can't grow past 632 without overlapping the intro — and its `z-index: 1` would then sit over the CTA. Its own `min()` ceiling moved 768 → 632 to match (the 768 was unreachable anyway: the old 820 cap capped the phone at 529).
    - **Open, needs a designer call:** to close the remaining 204px you'd have to either widen the band past 1312 (so the phone has more column to grow into), or let the band fill the viewport and spend the extra height on the intro/list rather than the phone. Both are layout decisions, not tuning.
    - Also noticed, **pre-existing, not touched**: `--dpc-phone-w` derives from the *assumed* `--dpc-stage-h`, so wherever content pushes the stage taller the phone under-fills — at 1280×800 the band runs 798 against a 680 assumption and the phone sits 115px clear top and bottom. Fixing it properly needs JS measurement, not CSS.
  - [x] **Earn tiles are `overflow: visible`** (item 2). The tint keeps its rounded corners either way — that's `border-radius`, not the clip — so this only lets art bleed past the tile, which is what a product shot with a soft shadow wants.
  - [ ] **⚠ ASSET REQUEST — `overflow` was not what was cutting the phone's shadow.** Nothing in CSS was clipping it: the art is `object-fit: contain`, which letterboxes it **~5px inside** the cell. **The shadow is cropped in the export**: `debit-earn-phone.png` is 1920×1104 and its alpha runs from row **0 to row 1103** — hard against both the top and bottom edges of the file. Needs a **re-export with transparent padding** around the art. (`debit-earn-platforms.png` has proper margins — insets T128 B29 L425 R440 — so it's only the phone.)
  - [ ] **Earn abonelik image to be replaced** (item 1) — owner is researching illustrations, **because the logos can't be used**. That answers the clearance question that has been open since 2026-08-03: **third-party marks are NOT cleared**, so `debit-earn-platforms.png` (Spotify, Amazon Prime, ChatGPT, Gemini) is a dead end, not a pending approval. Left on the page for now since a replacement is being sourced; the generic no-logo fallback is still recoverable from git at `6f634f8` (`illustrations/SubscriptionsMotif.tsx` + its CSS) if a placeholder is wanted before the new art lands. **Copy still names the three services in body text** — a lower-risk nominative use, but re-decide it with legal now that the marks are refused.
  - **Verification harness (worth keeping).** The preview pane still can't paint `<main>` on this page — `Reveal`'s IntersectionObserver never fires while the pane is hidden, so everything stays at opacity 0 and screenshots come back with only the header. Playwright isn't installed, but its **Chromium binaries are cached** and Node 24 ships a global `WebSocket`, so `scratchpad/cdp.mjs` drives real headless Chrome over CDP with **zero dependencies** (`launch/goto/eval/shot`), and `verify.mjs` measures this round's geometry at any viewport. Two traps: give each run a **unique `--user-data-dir`** (a locked profile from a previous run makes the debugger endpoint never come up), and run viewports **one process at a time** — back-to-back launches in a single shell loop fail.
  - **Pre-existing, flagged not fixed:** `npx eslint src/` reports **8 errors**, all in `src/components/` (`AnimatedNumber`, `AppFeatures`, `Calculator`, `Rates` — set-state-in-effect, refs-during-render, unescaped entities). None are in the files this round touched. Also: `.dpc-steps__phone`'s absolute placement is in a `@media (min-width: 768px)` block while its comment claims ≥921, so **timed mode gets desktop absolute positioning against the wrong containing block** (the stage is only `position: relative` in scrub) — measurable as a -77px bezel overhang at 900×800.

- [ ] **Hesap kartı — review feedback (2026-07-24)** — copy targets are in `src/data/content.ts`. **Do not merge to `main`** (page pending CMO/COO approval); implemented on the `gfdes-2174-feedback` branch, which now carries `main` merged in (2026-08-03) so the page is reviewed against the current header/menu chrome.

  Ready-to-implement items — done, on `gfdes-2174-feedback`:
  - [x] `DEBIT_HERO.sub` → "harcadıkça getirpara ve nakit iade kazan". ⚠ Still duplicates the `DEBIT_EARN` heading right below ("harcadıkça getirpara / ve nakit iade kazan") almost verbatim — implemented as given per instruction, flagging again here for owner sign-off before this ships.
  - [x] `DEBIT_EARN.sub` → "yemekten markete, akaryakıttan alışverişe nakit iade kazanırken; getirmarket ve bitaksi'de getirpara ile kazancını katla" ("su siparişinden" → "yemekten").
  - [x] `DEBIT_CAPS` "daha neler var?" moved to the bottom of the page (after abroad, before the footer). Sanal still renders immediately before abroad, so `CardHandoff`'s scroll-linked geometry (anchored to that adjacency) is unaffected.
  - [x] Yellow highlight bar descender clipping ("güvenle harca", "yapılacaklar listesi") — `.hl::after` (base.css) is tuned for non-descender text and was hiding most of the g/y tail under the bar. Fixed with a scoped override (`.dpc-sanal__head .hl::after`, `.dpc-abroad__title .hl::after` in `debit-current.css`), not a global base.css change, so the landing page and this page's two non-descender headings (earn, caps) keep the original tight bar. Verified via computed geometry (canvas TextMetrics + getBoundingClientRect) rather than a screenshot — the preview tool's screenshot capture wasn't rendering paint this session (DOM/CSS confirmed correct via JS; header painted, `<main>` didn't). **Worth a real-browser visual QA pass before this ships**, since verification here was numeric, not eyeballed.
  - [x] `DEBIT_SANAL.features` — swapped items 2 and 3 (now: ayrı kartlar oluştur → limitini belirle bütçeni kontrol et → internet alışverişlerini güvenle yap).

  Blocked on owner input (do NOT guess):
  - `DEBIT_CAPS`: add an "ücretsiz kart" item and change "para transferi" — final item set + copy TBD (currently: ücretsiz para transferi + anlaşmalı ATM).
  - ~~Add a **YouTube Premium** package to the earn area~~ — **superseded 2026-08-03** by the platform-subscriptions restructure below, which covers the same ground generically ("seçili dijital abonelikler") rather than per-service. Reopen only if YT specifically must be called out.
  - Delivery copy: "tek tık" / "tek dokunuş" sentences to change — current build only has step 1 "tek dokunuşla iste"; new wording TBD.
  - Delivery phone mockup **numbers** to update — which + target values TBD.
  - `DEBIT_ABROAD.captions`: drop the 3rd (ATM komisyon); 1st item → "yurtdışında rahatça kullanırsın" — replace-vs-prepend (2 vs 3 captions) TBD.

- [ ] **Earn section restructured to 3 benefits — platform subscriptions** (owner, 2026-08-03, Figma 22054:45003 "Platform Subs"). The two side-by-side cards became three: the new one is **%20 nakit iade on selected digital subscriptions**. Copy in `DEBIT_EARN.abonelik`.
  - **The three benefits are not peers**, which is what all three Figma variants assumed: `iade` covers *all* physical spend (broadest), `getirpara` and `abonelik` are narrow high-rate specifics. Owner chose (option 1) **iade leads**, so the default is an **unequal bento** — iade spans full width, the pair sits beneath. `.dpc-earn__bento`.
  - **`?earn=equal` keeps the peer-tile arrangement** for on-device comparison (owner wanted option 4 held as the alternative): three same-size portrait tiles, copy above media, `.dpc-earn__row`. Same link-only, no-picker pattern as `?steps=`. **Once a call is made:** delete the `?earn=` param + `getEarnParam` in `page.tsx`, the ternary on the `Reveal` className, and the loser's container rules in `debit-current.css`.
  - Read of the three comps, for the record: **A** (zigzag) gave all three cards equal size so the asymmetry was decorative, and put white copy over the photo's light sky with "iade!" running into the image; **B** (staircase) left ~40% of the section empty on the left with irregular step intervals; **C** (cardless rows) was structurally the cleanest but 2168px tall for three sentences, committed to no colour on a purple-drenched page, mixed three unrelated media kinds, and reused the ATM illustration that now sits in the caps section directly below. Bento lands at 973px at 1440 — roughly the old two-card height for one more benefit.
  - **No third-party marks** (owner, 2026-08-03). The comp's asset was a Spotify/Amazon Prime/ChatGPT logo cluster plus a yellow "20%" bubble; both dropped. The logos need a cleared source we don't have, and **the % bubble violates the AGENTS.md rule reserving % for interest/faiz** — on a cashback offer the sanctioned mark is the coin. So: rate lives in the copy as text (`.y` yellow span, same as the existing `%3`), cashback signal is `cashback.svg`, and the media is a new generic motif (`illustrations/SubscriptionsMotif.tsx`) — three abstract tiles with play / waveform / spark glyphs, universal media-category symbols standing in for no particular service. **Copy still names the three services in body text**, which is a lower-risk nominative use than reproducing marks but is flagged in `content.ts` for legal; "seçili dijital abonelikler" alone stands on its own if they object.
  - **⚠ `DEBIT_EARN.sub` now under-describes the section** — it names nakit iade and getirpara but not subscriptions. Needs a third clause or a rewrite; flagged in `content.ts`, not guessed.
  - Comps carry pre-2026-07-24 copy ("su siparişinden markete"); built against `content.ts`, not the comp.
  - Contrast fixed properly rather than scrimmed: the old blue card was `#7cc7f2→#4a9fd8`, which is **2.9:1 behind white** and leaned on a dark top scrim to stay legible. With copy in its own column the gradient carries it — stops are now `#2f6f9e → #33739b 55% → #4a9fd8 80% → #7cc7f2`, factored into `--dpc-iade-stops` so the three orientations (copy beside photo / above it on the peer row / above it on mobile) can't drift apart. **Measured on painted pixels, not computed values**: 5.21–5.27:1 on the blue tile and 7.29:1 on both purple tiles, across bento-1440 / bento-1024 / equal-1440. Method: transparent glyphs + sample the copy's *content* box only (the padding band overlaps the card's rounded corners, which are opaque page-white and read as a false 1.00:1).
  - **The in-app browser pane cannot paint `<main>` on this page** — same capture bug logged on the descender fix. Verified with headless Chromium via Playwright instead (script in the session scratchpad); that's a real render, so the earlier "numeric, not eyeballed" caveat is now closed for the earn section. Worth keeping the Playwright route for the rest of the page's QA.
  - Two `<path> attribute transform: Expected transform function, "undefined"` console errors on this page are **pre-existing** (confirmed by rendering with these changes stashed) — from `TransferIllustration`/`AtmIllustration`, not the new motif. Own item if worth fixing.
  - Not done: no motion on the new tile beyond the shared `Reveal` stagger. The bento's unequal shape invites a different entrance for the hero tile than the pair — micro-interactions pass.

- [ ] **Teslimatı section is a closed 1312 container, not a full-bleed band** (owner, 2026-08-03, page feedback at 2504). Full-bleed, the lilac ran to the viewport edges and the white below it read as a gap seaming into the dark sanal section; contained, that white is just page background around a card. Geometry as specified: 1312 max, so 64px margins at 1440.
  - The lilac fill + radius + inset now live on `.dpc-steps__band` for **all** modes (scrub / timed / off), and `.dpc-steps` is transparent. Mobile gets the same card treatment.
  - **Three traps hit while building it**, all worth knowing before touching this again:
    1. `.dpc-steps__pin` is a **column flex container**, and `margin-inline: auto` on a flex item overrides the default `stretch` — so `max-width: 1312px; margin-inline: auto` alone collapsed the band to its **content width (384px at 1440)**. `width: 100%` is load-bearing.
    2. With `width: 100%`, insetting below 1360 via side *margins* pushes the band 24px past the viewport. Inset via `max-width: calc(100% - 48px)` instead, keeping auto-margin centring as the single mechanism at every width.
    3. The band and the inner `.dpc-container` are both 1312, so content sat flush to the rounded edge and the ghost numerals got clipped. Inset lives on the band (`padding-inline: 64 / 32 / 20`) and `.dpc-steps__band > .dpc-container` is zeroed so it can't double up below 1360.
  - `overflow: clip` on the band is safe for the pin's stickiness — the sticky element is the *ancestor*, and clipping only breaks sticky when it's on an ancestor of the sticky node. The phone now clips to the card's right edge instead of bleeding to the viewport, which is the point.
  - **Removed pre-existing dead CSS** found here: `.dpc-steps[data-mode="scrub"] .dpc-steps__pin > .dpc-container { padding-block: 0 }` never matched — real nesting is `pin > band > container`, so the child combinator missed, and it was a no-op anyway since `.dpc-container` sets no block padding. The misleading "full-bleed width" comment on the old scrub band rule went with it.
  - `?steps=compact` (`DeliveryCompact.tsx`) uses none of these classes, so it's unaffected — it still needs its own pass if it ever wins the stacked-vs-compact call.

- [ ] **Earn layout v2 is now the default** (owner, 2026-08-03, Figma **22074:20218**). Owner's call after three rounds of comps; `?earn=bento` keeps the unequal bento for one comparison round, and the earlier equal-peer row is deleted (v2 supersedes it).
  - Why it won, measurably: the frame is **1440 with content at x=64 / width 1312 — exactly `.dpc-container`**, where the previous revision (22065:53577) was 1728 with a 1256 column and 13.7% margins that would have made the section narrower than every other section on the page. The row-1 gutter went **268px → 32px** (copy block 316 → 608 wide, 4 lines → 2), and 32px is now the gutter *everywhere* — copy→photo, tile→tile, row→row. Title dropped 48/56 → ~40/44 (matches `.dpc-title`) and the sub is 24/32 (matches `.dpc-earn__sub`), so no page-wide type decision is buried in it.
  - **Ink-on-tint replaces white-on-fill**, which is the real win: measured **18.2–19.3:1** across 1440 / 1024 / 390, versus the 5.2:1 the bento's blue needed a re-engineered gradient to reach. Tiles are `--gf-yellow-50` (**new token**, `#fffdf0` = `color/yellow/50`) and `--gf-lilac-50` (already existed).
  - Rate emphasis is **purple, not yellow** — yellow on those tints is invisible. All three benefits now share a `pre/em/post` shape in `content.ts` and one `.dpc-earn__em` span the layout colours (yellow in bento, purple in v2), so `₺1.250` gets emphasis in the lead row too.
  - Tile rows are `minmax(0,1fr) auto`, which reproduces the comp's 368/320 art-block split between the 2-line and 3-line tiles **without hardcoding either** — the art absorbs whatever the copy doesn't use.
  - `kart al` CTA deliberately **not** built (owner, 2026-08-03) — it was in 22065:53577 but not this frame; the section stays benefit-story only, CTA work sits in the hero and teslimatı.
  - Still the generic `SubscriptionsMotif`, not the comp's DS instance **`Illustration / Comm Area & Lottie / Platform Abonelik`** (329×329). Two things needed before that swaps in: confirmation the marks are cleared for the **public site** (a DS component shipping in the app is a much stronger position than an ad-hoc export, but app ≠ web), and the asset itself — it's a Lottie and isn't in `public/assets/lottie`. `lottie-web` is already wired, so it can animate when it lands.
  - **Copy drift the comp introduced, deliberately not followed:** the comp says "spotify, amazon ve dahası…" (drops chatgpt) while its illustration still shows the ChatGPT mark — text and image disagree. `content.ts` keeps all three named, which is consistent with the no-logo build. If the DS illustration lands, re-decide. (The comp's earlier `₺1250` separator loss is fixed in 22074:20218.)
  - **Assets now pulled from Figma directly** (owner, 2026-08-03) via the connector's `download_assets`, replacing the old 380×520 exports:
    - **Lead photo** → `debit-earn-photo.jpg`, Figma's own 2× crop of node `22074:20228` (1216×880). Shipped as **jpeg, not png** — it's a photograph and the CSS rounds it, so no transparency is needed: 257KB vs 1.8MB. The uncropped original in the file is 2160×2700 / 8.2MB, so don't reach for that. Old `debit-earn-photo.png` deleted.
    - **Phone** → `debit-earn-phone.png`, Figma export of node `22074:20231` at 2× (820×822). Its baked `#fffcf7` background was **flood-filled off from the edges only** — a global colour match would punch holes in the screen's own near-white UI — so it now works on the cream tint and the bento's purple alike. Fitted with `object-fit: cover` + bottom anchor: the export is near-square with transparent margin, so `contain` is height-bound and never fills the 391 art width (reads small); cover fills the width and takes ~42px off the top, which is what the comp does by bottom-aligning a 548-tall phone in a 368 frame.
    - **Superseded 2026-08-03 — owner exported all three manually at 3×**, replacing the connector pulls. All three are transparent-cornered, no baked backgrounds:
      - `debit-earn-photo.jpg` — 1824×1320. Exported as png (3.69MB) and **converted to jpeg (626KB)**; it's a photograph and the CSS rounds the corners, so the baked transparency was redundant. Re-export from Figma if the original is ever needed.
      - `debit-earn-phone.png` — 1920×1104, i.e. **the full 640×368 art area**, not the comp's 391 inner frame. Its near-opaque light block at the top measures `255,254,247` against the tile's `255,252,247` — a 2-unit difference, so it blends invisibly. No flood-fill needed this time.
      - `debit-earn-platforms.png` — 1920×1104, same full-art-area framing. **This is the logo cluster and it is now live on the page.**
      - Because both tile arts are authored at the tile's own width and aspect, the CSS lost the `min(391px)` inner-width block and the phone's cover crop — they fill the cell and `contain` seats them exactly. `SubscriptionsMotif.tsx`, `.dpc-earn__motif`, `.dpc-earn__coin` and the `:not(.dpc-earn__coin)` guard are all deleted as orphans.
    - **⚠ OPEN: third-party marks are now shipping in previews.** Spotify, Amazon Prime, ChatGPT and Gemini marks are in `debit-earn-platforms.png`. Clearance for the **public site** is still unconfirmed — a DS component shipping in the app is not the same permission. This branch is preview-only and **must not merge to `main`** until that's answered (it already can't, pending CMO/COO approval, so the two blockers coincide). If legal objects, the generic no-logo fallback is recoverable from git at `6f634f8` (`illustrations/SubscriptionsMotif.tsx` + its CSS), which needs the art rule pointed back at a 391 inner width.
    - Also note the comp's DS instance was named `... & Lottie ...` — if an animated version exists it would go in `public/assets/lottie/` and `lottie-web` is already wired; the still PNG is what shipped.
  - **`--gf-yellow-50` is `#fffcf7`, sampled from the comp's rendered tile — not the token export**, where `color/yellow/50` is `#fffdf0`, a visibly different cream. Measured because the Figma phone export's baked background was that same `#fffcf7`, so they sit seamlessly. **Worth a designer check: should the tile be the token or the comp?** (The abonelik tile's `#f9f7ff` *did* match `--gf-lilac-50` exactly.) This is the same category as the seven hand-picked off-export values in `design-tokens/README.md`.
  - **Type revised** (owner, 2026-08-03): benefit copy **34 → 28** at comp width (`clamp(20px, 1.95vw, 28px)`, line-height 1.25), and **weight splits by colour** — `--fw-regular` for the ink sentence, `--fw-semibold` only on the coloured figure, so the rate carries the emphasis instead of the whole line.
  - **Copy inset shares one token with the teslimatı band** (owner, 2026-08-03, page feedback at 1744): `--dpc-panel-pad` on `.dpc` (64 / 32 / 20 by breakpoint) drives both `.dpc-steps__band`'s `padding-inline` and `.dpc-earn__v2 .dpc-earn__copy`'s, so the two sections' content share a left edge and can't drift. Verified equal to the pixel at 1744 / 2504 / 1440 / 1024 / 390. Side effect: the narrower copy column pushes the abonelik sentence 2L → 3L at desktop, which the `1fr auto` tile rows absorb (the art shrinks) — no layout work needed.
  - **All three cells are now the same width** (owner, 2026-08-03, page feedback at 2504): the lead row uses the parent grid's `repeat(2, 1fr)` instead of the comp's 672/608 split, so the lead photo is exactly as wide as the abonelik tile below it and their edges line up. **This is a deliberate departure from the comp** — 22074:20218 draws the photo at 608 against its own 640 tiles, a 32px mismatch that reads as a broken right-hand column. Verified by measuring edges, not eyeballing: photo width/left/right all equal the abonelik tile's at 2504 / 1440 / 1024, with 32px gutters throughout.
  - **Mobile is derived, not comped** — no comp defines it for any variant. Lead row: copy then photo. Tiles: art above copy, tint kept. Trap hit and fixed: the blanket `@media ≤767` row template (`auto minmax(0,1fr)`) lost to v2's desktop `grid-row` assignments on specificity, so the art landed in the `auto` row and collapsed to zero height — the tiles rendered as empty tinted blocks. The mobile rule is now scoped to `.dpc-earn__bento` and v2 keeps its desktop row model.
  - Bento note, if it survives: its `₺1.250` is now yellow on the blue gradient at ~3.7:1 — passes AA large-text (24px semibold) but not body. Not tuned, since the variant is slated for deletion.
  - **Two verification-harness bugs worth remembering** (both produced confident wrong numbers): sampling a `deviceScaleFactor: 2` screenshot with CSS-pixel coordinates reads the top-left quarter of the intended region — scale by the DPR; and the dev-only `fixedMarkersLayer` (z-index 99998, viewport-covering, carries the floating dial/AI buttons) lands inside the copy box at some widths and reported a false 1.11:1 — hide non-shipping chrome before measuring. Line counts must use the content box, not the padding box.

- [x] **Hero + debit legal footnotes** (Aycan, Slack 2026-07-22) — added the Fibabanka rate legals to the hero and the delivery-time legal to the landing debit section:
  - hero: two rate legals under the Fibabanka line, **synced to the active offer** (`HeroOffer.legal` in content.ts) — deposit-rate legal on the %44 slide, kredi YMÖ example on the "iyi faizli kredi" slide, blank on kurlar/hesap kartı. New `.hero__disclosure` wraps the bank line + `.hero__legal`; bottom scrim on `.hero__media::after` for legibility; fixed min-height reserves the tallest legal so the bank line never jumps on swap
  - debit: `.debit__legal` muted footnote under the "keşfet" CTA
  - **copy normalization flagged for legal review**: lowercased "Örnek"→"örnek" to match brand voice, en-dash in the "2.500 TL – 4.500.000 TL" range, curly apostrophes — revert to Slack-verbatim if legal requires exact casing/punctuation
- [x] **Web sitesi revizyon round 1** (owner doc, 2026-07-21) — landed on the landing page:
  - copy: "şart yok" dropped (Faiz), footer download sub → "çok iyi faizli çok masrafsız…", stats → "türkiye'de ilk" + "her 4 getirfinanslı'dan 1'i" (hero badge unified to same wording), hero kredi slide → "iyi faizli kredi"
  - courier bigger (48→76px clamp) + slower (2.4s → 4s, underline draw re-synced)
  - yellow underline audit — rule: mark only under benefit statements. Removed: "kapattık!", "fırsatları", newsletter title. Kept: "her gün kazan!", "ücretsiz!", loan, debit, AI
  - stats legal footnote added (asterisk on 1'i + muted caption under the card)
  - hero slide 1 rebalanced: small "yıllık" cap above %44 (title now supports inline HTML)
  - rates "6091,23" typo in the feedback screenshot does **not** reproduce — current code renders thousands separators; stale Vercel deploy, fixed by deploying
- [ ] **Revizyon round 1 — parked: day→night transition** on "ışıkları kapattık" (feedback asks normal→dark mode switch as you scroll; current build only dims a dark photo 1.75→1). Blocked on a light-mode render of the same scene (`dark-mode-bg.png` has no light counterpart). Fallback if asset never lands: push the brightness/desaturate trick harder. **Direction to explore (owner, 2026-07-24): flip the *whole site* to dark (night) when the dark-mode section enters view, not just dim the one photo — plan this in a dedicated project session.**
- [x] **Mega menu — subtext fix** — new copy landed (2026-07-17) for hesap, hesap kartı, ihtiyaç kredisi, mini kredi, kasada kredi; added *para transferi* and *ücretsiz atm'ler* items (4th row)
- [x] **Menu revision** (owner, 2026-07-28) — on branch `menu-revision` (off `main`, deliberately not the blocked `gfdes-2174-feedback`). Nav is now data: `NAV` in `content.ts` feeds both the desktop bar and the ≤920 menu.
  - **Root problem found while scoping:** the mega panel was hardcoded JSX and rendered the *same* product grid whichever category you clicked, so on desktop "kampanyalar" and "ürün ve hizmet ücretleri" both opened products; at ≤920 `.nav` is hidden, so those two categories vanished entirely. Per owner: only `ürünler` has sub-items, the other two are plain links — so they now navigate on desktop and are peer rows in the menu.
  - Mobile menu: three peer rows, `ürünler` a Motion disclosure. **All three start collapsed** (owner, 2026-07-28, via `ui-skills pbakaus/distill`) — with subtexts on, an expanded `ürünler` runs ~1030px, pushing `kampanyalar` and `ücretler` below the fold where they effectively don't exist. Campaign discovery is a stated business goal, so the trade is one tap to products for two visible categories. All three rows show the same right-pointing chevron; only the disclosure's rotates down on expand, so they read as one set. Actions are a **sticky bottom bar** (badge + CTA) — they used to sit below 12 products, out of thumb reach.
  - **Subtexts are ON** (owner, 2026-07-28) — the CSS default at ≤920 shows them; ~8 of 12 items fit per screen at 375px, so the menu scrolls. **Tapping the `#paranaiyibak` badge in the menu toggles them** (owner, 2026-07-28) — DialKit's panel is unusable on a phone and this call needs judging on a real device. Uses `useDialKitController` so the badge and the panel drive one value instead of drifting. **Dev-only and verified against a real `next start` build**: in production the badge has no handler, no `title`, no `data-gf-dev-toggle`, and clicking it does nothing — never ship a brand mark that does something else when tapped. **Delete the hook, the effect + badge handler in `Header.tsx` and the `[data-gf-menu-subtexts="off"]` rule when settled**. Applied as a data attribute from a dev-gated effect, never gating JSX (the SSR hydration trap that bit `CampaignsCarousel`/`AbroadCollage`); dev-gated because `DialRoot`'s UI hides in prod but `useDialKit` still returns dial values. **Not persisted** — a stored value outlived the decision and masked the new default once already.
  - Bugs fixed: **Escape never closed the menu** (handler bound with `[]`, so `closeMega`'s guard always read a stale `false`); **the menu CTA never closed it** (`Button` dropped `onClick` whenever `href` was set — fixed for all call sites); logo painted over by the panel; hamburger never became an X; 768–920 had no CTA/badge at all; ~130px dead space from `padding-bottom: 80px`; `--ease-drawer` referenced but never defined. Added focus trap (compact only — a desktop dropdown isn't modal), `inert` when closed, `aria-expanded`/`aria-controls`, `role="dialog"`.
  - **CSS consolidated**: the menu had three fighting blocks (sections.css desktop + ≤920, mobile.css ≤767, with `!important` overriding `!important`). All menu rules now live in the single `@media (max-width: 920px)` block in sections.css — 920 is the real breakpoint since that's where `.nav` hides. `mobile.css` keeps only a `--menu-bar-h` override.
  - Type scale (owner, 2026-07-28): product labels bumped 18/26 → **22/30 to match the category rows**. Desktop mega untouched (24/32, `--gf-ink`). Expanded list is now ~1042px at 375px, which reinforces collapsed-by-default. `text/content/secondary` resolved via Figma `get_variable_defs` (`oQBanFVh7xrVxnYHokRLt7` / `21442-42526`) = `#3e3e3e` = `--gf-ink-700`; `tokens.css` annotates `--gf-ink`/`--gf-ink-700` with their confirmed Figma roles.
  - **Resolved 2026-07-29**: mobile product labels move to `text/content/primary` (`--gf-ink`, `#0e0e0e`), matching the desktop rule that was already primary. Was `--gf-ink-700`, sharing the subtext's colour with only size to tell them apart. Subtext keeps `--gf-ink-700` — it's now doing the quieting work the label used to, so level-2 items still read as one step down from `ürünler`.
  - Expanded `ürünler` drops to **`text/content/tertiary`** (owner, 2026-07-28), 250ms colour fade, so the header steps back once its items are showing — and the still-primary `kampanyalar` below reads stronger, which serves campaign discovery. **Resolved 2026-07-29** from the token export the owner supplied: `text/content/tertiary` = **`#8c96a4`** (`cool gray/400`), now `--gf-cool-400`. The earlier guess of `--gf-ink-400` (`#959595`) was **wrong** — tertiary is cool gray, not neutral, so the "continues the neutral ramp" reasoning was the wrong heuristic entirely.
  - [x] **Colour tokens in-repo** (owner, 2026-07-29) — `design-tokens/` holds the verbatim DTCG export from Figma: `primitives.json`, `semantic.Light.tokens.json`, `semantic.Dark.tokens.json`, plus a README. Reference material, **not** built code; nothing in `src/` imports it and `tokens.css` is not generated from it (`AGENTS.md`: the GF token names are load-bearing). Cross-checked every hex in `tokens.css` against it — two findings:
    - The `--gf-ink-*` family **spans two primitive ramps**: `-700` is `neutral/700`, `-500` is `cool gray/500`, `-400` is `neutral/500`. Not a consistent scale; documented inline rather than renamed (8+ call sites).
    - **Seven values aren't in the export at all** — `--gf-purple-700`, `--gf-purple-ink`, `--gf-yellow-600`, `--gf-card-purple`, `--gf-dark-bg`, `--gf-dark-bg-2`, `--gf-dark-surface`. Hand-picked in the vanilla build, mostly the dark app-split section. Left alone (design decision, not cleanup) but worth a designer pass.
    - `semantic.Dark.tokens.json` is unused; it's the starting point if the "flip the whole site dark on scroll" idea gets built, since role names match Light exactly.
  - Menu chrome (owner, 2026-07-28, revised same day): **no dividers between category rows at all** — spacing carries the separation; the only rule left in the menu is the sticky bar's top edge. **Only `ürünler` has a chevron**, so it reads as "this expands" rather than "this is tappable"; `kampanyalar`/`ücretler` are bare labels. Chevron centre lines up with the hamburger/X icon (`calc(22px - 4.5px)` — the 44px toggle is flush to the container edge so its icon centres 22px in; holds at any gutter). Superseded an earlier pass that gave all three rows chevrons + dividers.
    - **Watch:** with dividers gone and product labels at the same 22px, `kampanyalar` sits below the expanded product list separated only by whitespace and a colour step (`#0e0e0e` vs items' `#3e3e3e`). Reads acceptably because it has no subtext, but it's the weakest boundary in the menu — revisit if the category/product distinction tests poorly.
  - Motion pass done against emil-design-eng: disclosure spring tightened 420→280ms; hamburger↔X rebuilt as **transforms not `d` morphing** (the bar and diagonal use different path commands — `h` vs `l` — which doesn't interpolate reliably, and it was main-thread); menu exit 400→240ms (faster than enter); `:active` tint on rows. **`/review-animations` still owed — it's blocked from model invocation, owner needs to run it.**
  - Security review: 0 findings. `dangerouslySetInnerHTML` on `NavItem.sub` is safe only because `NAV` is static — needs a sanitizer if nav copy ever moves to a CMS.
  - Also fixed 2026-07-28: the hamburger's middle bar had no `initial` opacity, so Motion warned "animating opacity from undefined" and the first fade no-opped — the bar could survive into the X. Found in the dev-server log, not by looking.
  - **Testing note:** the in-app browser pane's viewport resize changes `innerWidth` and `matchMedia().matches` but fires **neither** `resize` nor matchMedia `change`, so the page never learns. Breakpoint-switch behaviour can't be tested by resizing — dispatch a synthetic `resize`, or reload at the target width. (Cost me a false "resize regression" diagnosis.) Same root cause bites animation: while the pane is hidden rAF stalls, so a mid-flight `getComputedStyle` reads frozen start values — it looked twice like the disclosure was clipping content at a pinned height when Motion actually settles it to `height: auto` correctly. Force a paint (screenshot) before measuring anything animated. Verified via synthetic event that when `compact` flips false mid-expand, React swaps `motion.div` → plain `div` and Motion's inline height goes with it, so the desktop dropdown can't inherit a clamped height.
  - Not done: panel open/close is still the CSS `.is-closing` + `setTimeout` state machine. Porting it to `AnimatePresence` would delete that machinery — deliberately deferred to avoid churning the tuned desktop dropdown.
- [x] **Motion integration** — landing page ported to Motion on the `motion-integration` branch (shared `Reveal` component replaces the GSAP reveals; ideation + build order in `docs/motion-ideation.md`). Remaining: `Loan` clip-path scroll-scrub still GSAP (port when touched)
- [x] **Yellow highlight — purple borders on light sections** — REVERTED (owner, 2026-07-17): highlight bar is yellow on all sections again; purple override removed from sections.css
- [ ] **Debit card page — finalize design** (`/hesap-karti`, GFDES-2174; layout locked "Dakikalar, drenched" → layout iteration → micro-interactions, built with Motion)
  - **Hero decided: C kart xl** (2026-07-10). Lab deleted (`HeroVariants.tsx`, `debit-hero-variants.css`, picker, `?hero=`); C lives as `Hero.tsx` with its geometry baked into `debit-current.css`, copy in `content.ts` (`DEBIT_HERO`). Next: micro-interactions pass. **No yellow highlight in the hero title** (owner, 2026-07-13) — the highlight is a sub-section device only.
  - Sections 2–7 layout iterated (fluid grid replaces the absolute comp): earn polished (scrim, ₺1.250), steps → "dakikalar" lilac field with ghost numerals + **Turkish draft copy (needs owner/legal review — replaced English template text)**, transfers/ATM/sanal consolidated into a capability bento with dark sanal panel, abroad collage now %-based (scroll strip ≤920). Copy moved to `content.ts` (`DEBIT_*`). Entrances via shared `Reveal`; `.hl` underlines draw on scroll like landing.
  - Header light-hero variant done properly: `<Header variant="inner" />` applies the vanilla build's `.site-header--inner` styles (ink nav, purple logo, CTA visible at top) — the `body[data-hero]` CSS patch is gone. Note: `--inner` also shows the header CTA before scroll, which the old patch didn't; intended per the vanilla design.
  - **DialKit installed** (`DialRoot` mounted in layout, dev-only) — application plan in `docs/dialkit-plan.md`: hero-lab geometry dials first, then shared motion dials (`src/dials/`) for the micro-interactions pass; tuned values get baked in and dials removed.
  - **Teslimatı compact layout variant** (`DeliveryCompact.tsx`, link-only via `?steps=compact`, Figma 21619:8182): lilac intro panel + bg-soft steps panel, phone on the seam. Shares the stacked layout's driver/mode/SEG machinery (exported from `DeliverySteps.tsx`); no bars/courier — the phone is the progress readout. **Waiting on the phone-screens lottie** (`PHONE_LOTTIE` → `/assets/lottie/steps-phone.json`, author screens to SEG thirds; driver-scrubbed via goToAndStop, static phone img until then). Pick stacked vs compact, then delete the loser + the param plumbing.
  - **Teslimatı scroll sequence built** (`DeliverySteps.tsx`): sticky-pinned scrub ≥921 / timed in-view run 768–920 / settled ≤767+reduced-motion. **Steps re-scripted + phone mock rebuilt** (owner, 2026-07-13): steps are now *kartını iste → ücretsiz kurye teslim etsin → kazanmaya başla* (desc lines are DRAFT, need review); the tilted PNG phone is replaced by a framed mock (`/assets/img/steps/frame.png`, transparent screen hole, Dynamic Island in the overlay) whose app screens (ürün seçimi / kurye takip / harcarken kazandıkların) slide in from the right per step with iOS-push parallax (outgoing screen recedes −22%), driver-bound. **Phone lift** (owner, 2026-07-14): the mock rests bottom-cropped on step 1, rises over the step-2 window until its bottom clears the stage (lift distance measured via offset chain + ResizeObserver, 40px inset) and holds through step 3. **Height model fixed** (owner, 2026-07-14): the pinned section used raw `100vh`, so phone-size (vw) and window (vh) fought — on tall screens the whole phone fit and the lift collapsed to 0. **Section wraps content, no 100vh fill** (owner, 2026-07-14): pin is `sticky` and hugs its container (no `100vh`); `.dpc-container` has 80px vertical padding and is the full section height; a new inner `.dpc-steps__stage` is the phone-driven clip window (`--dpc-phone-w × 1.016 ≈ 60% of phone height`, clamped 560–780, capped `100vh − 200px` so stage+padding fits). ~54–60% of the phone shows on every screen, lift ~530–610px, measured against the stage. Scrub track bg is lilac so the wrapped (shorter) pin leaves no bare gap on tall screens — steps 2/3 carry their payoff in the screens' bottom drawers. Motion 10/10 pass done per emil-design-eng/review-animations: full transform strings everywhere (no x/y shorthands on scroll-linked motion), check lottie at 1.4×, rewards pop with 0.35 overshoot, will-change on the sliding screens. `debit-phone-tilted.png` is now unused. **Completion marks now inline with step titles** (owner, 2026-07-13): step 1 plays the supplied check lottie (`/assets/lottie/check.json`, fires at done1, resets on scroll-up); step 3 pops + bounces the getirpara/cashback icons at done3 — no step-3 lottie needed anymore. Lottie dependency closed. Courier ported from landing `.debit__moto` (same curve/bob/travel pattern). Scrub feel needs a real-browser QA pass (headless can't scroll) — good first DialKit candidate.
  - **Abroad collage stagger** (`AbroadCollage.tsx`): card entrance completes → Lisbon → Berlin → rest shuffled once per load, 80ms stagger, never re-triggers. DOM order is fixed and the randomness lives in the delays — shuffling in render caused an SSR/client hydration mismatch (same trap as `CampaignsCarousel`).
  - **Sanal kart is now its own section** (`.dpc-sanal`, `#sanal-kart`, full-bleed dark like landing `.debit`) — it's a distinct product, not an attribute of the physical card. Heading `sanal hesap kartıyla / güvenle harca` is a **draft, needs owner review**. Section is anchor/deep-link ready if it ever gets its own page.
  - **Sanal section redesigned to Figma final** (21830:9026, 2026-07-14): centered stack (heading → `kart al` CTA → card → 3 glass feature cards), ring field + CSS glow behind the card, bottom vignette. Card asset is now a **flat SVG** (`debit-sanal-card.svg`, derived by counter-rotating the Figma export; old PNG deleted) tilted -20° in code. Gotcha logged: the CSS minifier drops `backdrop-filter` if it comes *before* `-webkit-backdrop-filter` — keep the webkit prefix first, matching the rest of the codebase.
  - **Sanal→abroad card handoff built** (`CardHandoff.tsx`, owner direction 2026-07-14, replaced the round-1 pinned-morph): a shared-element transition — when the sanal section top hits the viewport top the virtual card SNAPS (spring, not scrub) off the page to viewport center, un-tilting -20°→0° and shrinking to the abroad collage card slot's size; it floats there while the section scrolls past, then rides down with the scroll, crossfading into the physical render mid-descent, and **lands as `.dpc-abroad__img-card`** (the collage's own card — one continuous object across sections). No added track: rides the natural scroll distance. Mechanics: fixed "traveler" overlay, snap progress `s` (spring on anchor crossing) × descent progress `d` (pure function of the slot's live viewport position; `d×s` so flicks can't outrun the snap; landing is exact by construction). Phase store (idle/flying/landed) coordinates: SanalCard hides its in-flow stack, AbroadCollage hides its card until landed and keys the photo stagger to the landing (original entrance kept below 921/reduced-motion — the handoff maps timed→off). **Motion gotcha logged**: `useTransform(fn)` dependency auto-collection proved unreliable with geometry not ready at mount (computed value froze) — use the explicit array-dependency form for scroll-critical chains, and never trust `.get()` on a computed value pre-subscription. Feel (snap spring, LAND_AT 0.62, fade windows) untuned — DialKit candidates for the real-browser QA pass.
  - **Placeholder assets filled** (2026-07-13): transfer + ATM bento slots use owner-supplied SVGs (both 600×440): `debit-cap-transfer.svg` (paper plane + trail over half-yellow globe; slot keeps native aspect so the composition isn't crop-severed) and `debit-cap-atm.svg` (ATM-pin map; fills the card via cover, side-crop tolerant). Sanal panel uses DS **"Sanal Hesap Kartı v2"** (Dark variant, circle bg + frame fill stripped for a transparent cutout → `debit-sanal-card.png`; exported via a temp instance on the Cover page, since removed). Dashed wireframe borders dropped from all three media slots.

## Blocker

- [x] **Vercel 404** — resolved (Framework Preset set to Next.js, Deployment Protection off). Live and serving `main`: https://gf-landing-prototype.vercel.app/hesap-karti

## Bugs

- [x] `CampaignsCarousel` hydration mismatch — fixed on `motion-integration`: pos classes derive from state in render, no `window` reads
- [x] `Rates` GSAP "target not found" — gone with the Motion port (`motion-integration`); GSAP removed from Rates entirely
- [x] `AbroadCollage` hydration mismatch — shuffling during render made the prerendered order differ from the client (and vary per request); DOM order is now fixed and the randomness lives in the stagger delays, set in an event callback
- [x] `HeroVariants` `setState`-in-effect lint error reading `?hero=` — replaced with `useSyncExternalStore`

## Backlog

- [ ] **Newsletter CTA URL** — the section is now a static promo with marketing's flat newspaper render (`ekspres-sayi-004.png`, replaced the fanned page stack 2026-07-22; page-peel scrub removed 2026-07-21). The "gazeteyi oku" CTA and the image link point at `#` until the real newsletter destination exists (`NEWSLETTER.href` in content.ts). Old fanned-stack assets (`sayfa-0N@2x.webp`) are now unused; the marketing PNG is 1.5 MB (transparent, no webp tooling locally) — ask marketing for a lighter/webp export
- [ ] **Newsletter issue line vs asset** — copy shows "sayı 01 · temmuz 2026" but the new masthead render says "sayı 004"; confirm whether the `issue` line in content.ts should update to match
- [ ] **Faiz phones — mobile asset** — desktop got the new %44,00 visual (2x, top-cropped); `faiz-phones-mobile.png` (≤767px) still shows the old design, needs a matching export
- [ ] Wire AI assistant mobile lottie (`ai-assistant-mobile.json`, slot framed 4:5) once exported
- [ ] GFDES-2135 revisions (source: Figma comments on the dev branch): hero flips title **and** subtitle through 4 offer pairs; FX cards drop average rate, push al/sat forward (needs design exploration); stats section gets a title, keep the 1.000.000+ badge; moped animates along the yellow underline; remove bottom "kendi uygulamasında" CTA
- [ ] 10/10 review pass — critique the page with design skills (animations, web design guidelines), produce findings, close them
- [ ] Consolidate the two mobile CSS layers (920 → 767)
